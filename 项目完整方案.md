# 智能库存归因 Agent：完整项目方案

## 1. 项目定位

### 项目名称

中文：企业智能库存异常归因 Agent  
英文：Enterprise Inventory Root-Cause Agent

### 一句话介绍

面向制造业库存管理场景，通过结构化工具调用、库存指标计算和业务图谱多跳追溯，识别呆滞库存风险，输出带证据链的归因结论与处理建议。

### 项目目标

本项目不是通用聊天机器人，也不是复刻公司内部系统。它要证明以下能力：

1. 能把自然语言问题转换成可靠的工具调用。
2. 能对结构化业务数据进行确定性计算，而不是让大模型编造数据。
3. 能通过最小业务图追踪“采购/生产订单—物料—仓库”关系。
4. 能输出可解释、可追溯的归因证据。
5. 能通过测试集量化 Agent 的工具选择和任务完成效果。

## 2. 使用场景

系统支持以下典型问题：

- 最近 90 天哪些物料存在呆滞风险？
- 物料 MAT-001 的库存为什么持续升高？
- 某批库存与哪些采购单和生产订单有关？
- 是需求下降、超量采购还是生产延期导致了积压？
- 请给出归因结论、数据证据和建议动作。

## 3. 范围边界

### MVP 必须实现

- FastAPI 后端与健康检查接口。
- 基于 SQLite 的合成业务数据。
- 风险清单、单物料归因、证据追踪三个用例级 Tool。
- 基于 LangGraph 或轻量状态机的可控工作流。
- 统一请求、响应、错误码、日志与 Trace ID。
- pytest 单元测试与 API 集成测试。
- 至少 12 条 Agent 评测样例。
- 一个轻量演示页面。
- Docker、README、架构图和演示脚本。

### MVP 暂不实现

- 真实 ERP 系统接入。
- 公司内部数据、代码、表名或客户信息。
- 多租户生产级权限系统。
- 复杂微服务拆分和高并发集群。
- 从零训练或微调大模型。
- 销售取消、ECN、供应商分析和完整生产制造模型。
- BOM、工序、工作中心、生产领退料、物料预留和状态历史。
- 为堆栈而引入 Redis、消息队列或外部图数据库。

## 4. 业务数据模型

所有数据均为自行生成的脱敏合成数据。

### 核心实体

| 实体 | 关键字段 | 作用 |
|---|---|---|
| Material | material_id、name、category、unit_cost | 物料主数据 |
| Warehouse | warehouse_id、name、region | 仓库信息 |
| InventoryMovement | movement_id、material_id、warehouse_id、movement_type、quantity、posted_at | 库存计算事实 |
| PurchaseOrder | po_id、material_id、warehouse_id、ordered_qty、received_qty、planned_date、actual_date | 采购供给证据 |
| ProductionOrder | production_order_id、material_id、warehouse_id、planned_consumption_qty、status、planned_start、actual_start | 生产延期证据 |

库存余额从 `InventoryMovement` 聚合，首版不建快照表；图边从业务外键派生，首版不建通用关系表。`ProductionOrder.material_id` 直接表示计划消耗的一种物料，明确不模拟完整 BOM。

### 图关系

```text
PurchaseOrder --PURCHASES--> Material
ProductionOrder --CONSUMES--> Material
Material --STORED_IN--> Warehouse
```

MVP 可以使用 NetworkX 作为本地图适配器，保证零外部依赖运行；扩展阶段增加 Neo4j 适配器，体现图数据库能力。业务层不得直接依赖具体图库。

## 5. 呆滞库存定义

项目必须明确业务口径，避免“让 LLM 自己判断”。建议 MVP 规则：

```text
呆滞风险 = 当前库存 > 0
        AND 最近 N 天出库量低于阈值
        AND 库存覆盖天数高于阈值
```

关键指标：

- 平均日需求 = 最近 N 天销售出库量 / N。
- 库存覆盖天数 = 当前库存 / 平均日需求。
- 库存周转率 = 指定周期销售成本 / 平均库存金额。
- 库龄 = 当前日期 - 最近一次有效出库日期。
- 呆滞金额 = 呆滞数量 × 单位成本。

对零需求、缺失记录、负库存和日期异常必须定义显式处理规则。

## 6. 归因规则

第一版采用可解释规则评分，不让 LLM 直接决定根因。

| 根因   | 证据示例                | 初始评分规则示例          |
| ---- | ------------------- | ----------------- |
| 需求下降 | 近 30 天需求较前 30 天显著下降 | 降幅超过 40% 加分       |
| 超量采购 | 未消耗采购量明显高于预测需求      | 采购覆盖超过 120 天加分    |
| 生产延期 | 生产计划消耗未按期发生、订单仍未关闭 | 延期超过 14 天且当前仍有库存时加分 |

归因结果必须包含：根因类型、置信分、证据列表、数据来源、计算口径和建议动作。

## 7. 工具设计

### Tool 1：list_inventory_risks

- 输入：仓库、物料类别、分析日期。
- 输出：风险物料清单、关键指标和数据状态。
- 责任：完成“查哪些物料有风险”的完整用例。

### Tool 2：analyze_material_root_cause

- 输入：物料、仓库、分析日期。
- 输出：确定性指标、三类候选根因、命中规则和证据。
- 责任：编排内部 Service，不把评分交给 LLM。

### Tool 3：trace_evidence

- 输入：物料、仓库、证据类型、最大跳数。
- 输出：采购单/生产单节点、受控关系和路径。
- 责任：面向人工核对限制跳数、节点数和执行时间。

指标计算与单条根因规则是 `app/services` 内部能力，不为展示 Tool 数量而逐个注册。报告组织属于 Agent 最终节点，也不单独注册为 Tool。

## 8. Agent 工作流

```text
用户问题
  -> 意图识别与参数抽取
  -> 恢复已确认会话参数
  -> 参数/范围校验
  -> 选择用例级 Tool
  -> 执行确定性查询、计算和归因
  -> 校验数字与证据
  -> 生成带证据的回答
```

### 状态节点

- parse_request：提取 material_id、warehouse_id、as_of_date 和分析意图。
- restore_context：只恢复当前会话已确认参数，并设置 TTL 和最大轮数。
- validate_request：使用 Pydantic 校验，缺少关键参数时返回澄清问题。
- route_tool：从三个用例级 Tool 中选择一个。
- execute_tool：执行 Tool；业务计算继续由确定性 Service 完成。
- validate_evidence：检查每条结论是否存在证据。
- compose_answer：将结构化结果转换成自然语言。

### 失败与降级

- 参数缺失：要求用户补充，不猜测物料。
- LLM 不可用：仍返回结构化指标和规则归因结果。
- 图谱无关联：降级为结构化数据分析，并说明证据不足。
- 数据为空：返回可解释的业务错误，不生成结论。
- Tool 超时：有限次数重试，记录 Trace ID。
- Agent 达到最大执行步数：停止继续调用并返回可解释降级结果。

## 9. API 设计

| 方法   | 路径                               | 作用       |
| ---- | -------------------------------- | -------- |
| GET  | /health                          | 服务健康检查   |
| POST | /api/v1/chat                     | 自然语言分析入口 |
| POST | /api/v1/analysis                 | 结构化分析入口  |

统一响应结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "trace_id": "..."
}
```

## 10. 技术架构

```text
Streamlit / 简单 Web UI
          |
       FastAPI
          |
  Agent Workflow / LangGraph
     |        |         |
 Inventory  Metrics   Graph/Root Cause Tools
     |                  |
  SQLite           NetworkX / Neo4j Adapter
          |
  Logging + Trace + Evaluation
```

最终技术栈：Python 3.11、FastAPI、Pydantic v2、SQLAlchemy、SQLite、LangGraph、NetworkX、pytest、httpx2、Streamlit、Docker Compose。

## 11. 目录设计

```text
inventory-root-cause-agent/
├─ app/
│  ├─ api/
│  ├─ agent/
│  ├─ core/
│  ├─ domain/
│  ├─ repositories/
│  ├─ services/
│  ├─ synthetic/
│  ├─ tools/
│  └─ ui/
├─ benchmarks/
│  └─ results/
├─ evals/
│  ├─ datasets/
│  ├─ runners/
│  └─ results/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  ├─ e2e/
│  ├─ evals/
│  └─ benchmarks/
├─ reports/
├─ docs/
├─ scripts/
├─ .env.example
├─ .dockerignore
├─ pyproject.toml
├─ requirements.lock
├─ Dockerfile
├─ docker-compose.yml
└─ README.md
```

## 12. 测试与评测

### 工程测试

- 指标公式的正常、边界和异常用例。
- 归因规则逐项命中与不命中用例。
- Repository 和 Graph Adapter 测试。
- FastAPI 请求、响应和错误码测试。
- 完整归因链路端到端测试。

### Agent 评测

至少准备 12 条问题，覆盖：

- 工具选择准确率。
- 参数抽取准确率。
- 任务完成率。
- 证据引用正确率。
- 无答案时的拒答/降级正确率。
- 缺失仓库或日期时的两轮参数补全。

第一版目标不是虚构高指标，而是先建立可复现基线，再如实记录优化前后变化。

## 13. 安全与合规

- 仅使用合成数据。
- `.env` 存储密钥，仓库仅提交 `.env.example`。
- 日志不记录密钥和完整敏感输入。
- 对查询参数做白名单与长度限制。
- 不展示模型私有思维过程，只展示工具调用摘要和证据。
- README 明确项目与原实习公司的数据、代码无关。

## 14. 分阶段交付

### Phase 0：设计冻结（0.5 天）

- 确认业务口径、实体模型、工具契约和验收标准。

### Phase 1：可运行数据与后端（2 天）

- 创建工程、合成数据、SQLite、Repository、FastAPI。

### Phase 2：指标、三类归因与证据图（2～3 天）

- 完成三个用例级 Tool、三类根因规则、NetworkX 证据图和单元测试。

### Phase 3：Agent 工作流（2 天）

- 完成参数提取、状态编排、校验、降级和执行轨迹。

### Phase 4：评测与前端（2 天）

- 建评测集、运行基线、完成演示页面。

### Phase 5：求职交付物（1 天）

- README、架构图、自动演示脚本、Docker/Compose、简历表述、项目问答和版本发布。

## 15. 完成标准

- 新环境按 README 能启动。
- 无 LLM 密钥时核心确定性分析仍可运行。
- 关键业务规则具有自动化测试。
- 每个结论能追溯到指标或图谱证据。
- 至少 12 条评测样例可重复运行。
- 自动演示能重复验证正常、多根因、证据路径和无 LLM/空结果链路；人工演示可在 2 分钟内选择正常与降级场景完成。
- 简历上的每个指标都能在代码、测试报告或评测报告中找到依据。

## 16. 最终收尾结果（2026-08-16）

- Phase 0～5 全部完成，Phase 4 保持“评测与前端”，求职、容器、文档和发布交付归入 Phase 5。
- 230 项测试通过，`app` 分支覆盖率 91.29%，90% 门槛通过。
- 15 条 Agent 评测、4/4 自动演示、Ruff、依赖检查、安全扫描、性能和冷启动均通过。
- Docker Compose API/UI 双服务完成镜像构建、健康检查、无密钥 Agent 请求和非 root 运行验收。
- 原 MVP 功能版本为 `v0.1.0`；补齐 Phase 5 与全文件收尾后发布 `v0.1.1`。
