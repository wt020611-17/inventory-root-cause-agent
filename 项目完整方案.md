# 智能库存归因 Agent：完整项目方案

## 1. 项目定位

### 项目名称

中文：企业智能库存异常归因 Agent  
英文：Enterprise Inventory Root-Cause Agent

### 一句话介绍

面向制造业库存管理场景，通过结构化工具调用、库存指标计算和业务图谱多跳追溯，识别呆滞库存风险，输出带证据链的归因结论与处理建议。

### 项目目标

本项目不是通用聊天机器人，也不是复刻公司内部系统。它要证明以下能力：

1. 能把自然语言问题转换成可靠的工具调用。
2. 能对结构化业务数据进行确定性计算，而不是让大模型编造数据。
3. 能通过知识图谱追踪“物料—采购—生产—销售—变更”关系。
4. 能输出可解释、可追溯的归因证据。
5. 能通过测试集量化 Agent 的工具选择和任务完成效果。

## 2. 使用场景

系统支持以下典型问题：

- 最近 90 天哪些物料存在呆滞风险？
- 物料 MAT-001 的库存为什么持续升高？
- 某批库存与哪些采购单、生产订单和销售订单有关？
- 是需求下降、超量采购、生产延期还是工程变更导致了积压？
- 请给出归因结论、数据证据和建议动作。

## 3. 范围边界

### MVP 必须实现

- FastAPI 后端与健康检查接口。
- 基于 SQLite 的合成业务数据。
- 库存查询、周转计算、图谱追溯、归因分析四类工具。
- 基于 LangGraph 或轻量状态机的可控工作流。
- 统一请求、响应、错误码、日志与 Trace ID。
- pytest 单元测试与 API 集成测试。
- 至少 20 条 Agent 评测样例。
- 一个轻量演示页面。
- Docker、README、架构图和演示脚本。

### MVP 暂不实现

- 真实 ERP 系统接入。
- 公司内部数据、代码、表名或客户信息。
- 多租户生产级权限系统。
- 复杂微服务拆分和高并发集群。
- 从零训练或微调大模型。

## 4. 业务数据模型

所有数据均为自行生成的脱敏合成数据。

### 核心实体

| 实体 | 关键字段 | 作用 |
|---|---|---|
| Material | material_id、name、category、unit_cost | 物料主数据 |
| InventorySnapshot | material_id、warehouse_id、quantity、snapshot_date | 库存快照 |
| PurchaseOrder | po_id、material_id、quantity、planned_date、actual_date | 采购供给 |
| ProductionOrder | production_id、material_id、quantity、status、due_date | 生产计划 |
| SalesOrder | sales_id、material_id、quantity、status、delivery_date | 销售需求 |
| EngineeringChange | ecn_id、material_id、change_type、effective_date | 工程变更 |
| Supplier | supplier_id、name、lead_time_days | 供应商信息 |
| Warehouse | warehouse_id、name、region | 仓库信息 |

### 图关系

```text
Supplier --SUPPLIES--> Material
PurchaseOrder --PURCHASES--> Material
ProductionOrder --CONSUMES/PRODUCES--> Material
SalesOrder --DEMANDS--> Material
EngineeringChange --AFFECTS--> Material
Material --STORED_IN--> Warehouse
```

MVP 可以使用 NetworkX 作为本地图适配器，保证零外部依赖运行；扩展阶段增加 Neo4j 适配器，体现图数据库能力。业务层不得直接依赖具体图库。

## 5. 呆滞库存定义

项目必须明确业务口径，避免“让 LLM 自己判断”。建议 MVP 规则：

```text
呆滞风险 = 当前库存 > 0
        AND 最近 N 天出库量低于阈值
        AND 库存覆盖天数高于阈值
```

关键指标：

- 平均日需求 = 最近 N 天销售出库量 / N。
- 库存覆盖天数 = 当前库存 / 平均日需求。
- 库存周转率 = 指定周期销售成本 / 平均库存金额。
- 库龄 = 当前日期 - 最近一次有效出库日期。
- 呆滞金额 = 呆滞数量 × 单位成本。

对零需求、缺失记录、负库存和日期异常必须定义显式处理规则。

## 6. 归因规则

第一版采用可解释规则评分，不让 LLM 直接决定根因。

| 根因   | 证据示例                | 初始评分规则示例          |
| ---- | ------------------- | ----------------- |
| 需求下降 | 近 30 天需求较前 30 天显著下降 | 降幅超过 40% 加分       |
| 超量采购 | 未消耗采购量明显高于预测需求      | 采购覆盖超过 120 天加分    |
| 生产延期 | 关联生产订单延期或长期未关闭      | 延期超过 14 天加分       |
| 工程变更 | ECN 导致物料替换、停用或版本变化  | 呆滞形成前后存在有效 ECN 加分 |
| 销售取消 | 关联销售订单取消或减少         | 取消量占库存比例较高时加分     |

归因结果必须包含：根因类型、置信分、证据列表、数据来源、计算口径和建议动作。

## 7. 工具设计

### Tool 1：query_inventory

- 输入：物料、仓库、日期范围。
- 输出：当前库存、库存金额、最近出库日期、数据完整性标记。
- 责任：只查询，不作主观归因。

### Tool 2：calculate_inventory_metrics

- 输入：物料、分析窗口、阈值配置。
- 输出：平均日需求、覆盖天数、周转率、库龄、呆滞风险。
- 责任：执行确定性计算。

### Tool 3：trace_business_graph

- 输入：物料 ID、关系类型、最大跳数。
- 输出：相关采购、生产、销售、ECN 节点及关系路径。
- 责任：给出图谱证据链，限制最大跳数与节点数。

### Tool 4：analyze_root_causes

- 输入：库存指标与图谱证据。
- 输出：候选根因、规则命中情况、得分及建议。
- 责任：基于规则引擎归因。

### Tool 5：generate_analysis_report

- 输入：结构化分析结果。
- 输出：面向用户的摘要、证据和建议。
- 责任：LLM 只做语言组织，不改变数值与根因得分。

## 8. Agent 工作流

```text
用户问题
  -> 意图识别与参数抽取
  -> 参数校验/歧义检查
  -> 查询库存
  -> 计算指标
  -> 按需追踪业务图谱
  -> 规则归因
  -> 结果校验
  -> 生成带证据的回答
```

### 状态节点

- parse_request：提取 material_id、日期范围和分析意图。
- validate_request：使用 Pydantic 校验，缺少关键参数时返回澄清问题。
- fetch_inventory：调用库存查询工具。
- compute_metrics：执行指标计算。
- trace_relations：只有归因任务才进行图谱追溯。
- score_causes：执行确定性规则评分。
- validate_evidence：检查每条结论是否存在证据。
- compose_answer：将结构化结果转换成自然语言。

### 失败与降级

- 参数缺失：要求用户补充，不猜测物料。
- LLM 不可用：仍返回结构化指标和规则归因结果。
- 图谱无关联：降级为结构化数据分析，并说明证据不足。
- 数据为空：返回可解释的业务错误，不生成结论。
- Tool 超时：有限次数重试，记录 Trace ID。

## 9. API 设计

| 方法   | 路径                               | 作用       |
| ---- | -------------------------------- | -------- |
| GET  | /health                          | 服务健康检查   |
| GET  | /api/v1/tools                    | 查看工具元数据  |
| POST | /api/v1/chat                     | 自然语言分析入口 |
| POST | /api/v1/analysis                 | 结构化分析入口  |
| GET  | /api/v1/materials/{id}/inventory | 查询库存详情   |
| GET  | /api/v1/traces/{trace_id}        | 查看执行轨迹   |

统一响应结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "trace_id": "..."
}
```

## 10. 技术架构

```text
Streamlit / 简单 Web UI
          |
       FastAPI
          |
  Agent Workflow / LangGraph
     |        |         |
 Inventory  Metrics   Graph/Root Cause Tools
     |                  |
  SQLite           NetworkX / Neo4j Adapter
          |
  Logging + Trace + Evaluation
```

推荐技术栈：Python 3.11、FastAPI、Pydantic v2、SQLAlchemy、SQLite、LangGraph、NetworkX、pytest、httpx、Streamlit、Docker。

## 11. 目录设计

```text
智能库存归因Agent/
├─ app/
│  ├─ api/
│  ├─ agent/
│  ├─ core/
│  ├─ domain/
│  ├─ services/
│  ├─ repositories/
│  ├─ tools/
│  └─ graph/
├─ data/
│  ├─ synthetic/
│  └─ seeds/
├─ evals/
│  ├─ datasets/
│  └─ runners/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ e2e/
├─ frontend/
├─ docs/
├─ scripts/
├─ .env.example
├─ pyproject.toml
├─ Dockerfile
├─ docker-compose.yml
└─ README.md
```

## 12. 测试与评测

### 工程测试

- 指标公式的正常、边界和异常用例。
- 归因规则逐项命中与不命中用例。
- Repository 和 Graph Adapter 测试。
- FastAPI 请求、响应和错误码测试。
- 完整归因链路端到端测试。

### Agent 评测

至少准备 20 条问题，覆盖：

- 工具选择准确率。
- 参数抽取准确率。
- 任务完成率。
- 证据引用正确率。
- 无答案时的拒答/降级正确率。

第一版目标不是虚构高指标，而是先建立可复现基线，再如实记录优化前后变化。

## 13. 安全与合规

- 仅使用合成数据。
- `.env` 存储密钥，仓库仅提交 `.env.example`。
- 日志不记录密钥和完整敏感输入。
- 对查询参数做白名单与长度限制。
- 不展示模型私有思维过程，只展示工具调用摘要和证据。
- README 明确项目与原实习公司的数据、代码无关。

## 14. 分阶段交付

### Phase 0：设计冻结（0.5 天）

- 确认业务口径、实体模型、工具契约和验收标准。

### Phase 1：可运行数据与后端（2 天）

- 创建工程、合成数据、SQLite、Repository、FastAPI。

### Phase 2：指标与归因工具（2 天）

- 完成四类核心工具、规则引擎和单元测试。

### Phase 3：Agent 工作流（2 天）

- 完成参数提取、状态编排、校验、降级和执行轨迹。

### Phase 4：评测与前端（2 天）

- 建评测集、运行基线、完成演示页面。

### Phase 5：求职交付物（1 天）

- README、架构图、演示脚本、简历表述、项目问答。

## 15. 完成标准

- 新环境按 README 能启动。
- 无 LLM 密钥时核心确定性分析仍可运行。
- 关键业务规则具有自动化测试。
- 每个结论能追溯到指标或图谱证据。
- 至少 20 条评测样例可重复运行。
- 演示能在 2 分钟内展示正常链路与一次降级链路。
- 简历上的每个指标都能在代码、测试报告或评测报告中找到依据。
