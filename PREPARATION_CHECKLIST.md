# Phase 0 开工清单

更新时间：2026-08-09

## 已完成

- [x] 复用现有“智能库存归因Agent”目录，不重复新建同类项目。
- [x] 明确项目业务名称为“呆滞料智能体”。
- [x] 明确仅使用合成数据和只读分析。
- [x] 明确 MVP 用户、范围、非范围和成功标准。
- [x] 明确分析粒度为“物料 × 仓库 × 分析日期”。
- [x] 定义默认指标、风险规则和异常边界。
- [x] 将 MVP 收缩为需求下降、超量采购、生产延期三类候选根因。
- [x] 将 MVP 收缩为五个持久化实体、四类图节点和三类图关系。
- [x] 明确生产延期使用简化生产单模型，不扩展 BOM、工序和领退料体系。
- [x] 定义数据实体、枚举、七个必造场景和质量检查。
- [x] 定义功能、测试、非功能和演示验收标准。
- [x] 修正 README 中与实际文件不一致的“Day 1 已完成代码”描述。

## 开发前阻塞项

- [x] 接受 `docs/01_业务口径与需求.md` 的默认阈值作为合成数据 MVP v0.2 基线，并保留配置化能力。
- [x] 安装并指定 Python 3.11.9。
- [x] 在正式项目目录初始化独立 Git 仓库。
- [x] 建立隔离虚拟环境并安装 `pyproject.toml` 开发依赖。
- [x] 运行依赖导入检查。
- [x] 通过 `requirements.lock` 锁定并记录 Python 3.11.9 已验证环境的精确依赖版本。

已确认 Git 2.55.0 和 Docker 29.6.2 CLI 可用；它们不是进入领域模型开发的当前阻塞项。

## Phase 1 建议任务顺序

1. 建立 `app/domain`、`app/repositories`、`app/services` 和 `tests` 包结构。
2. 实现领域枚举、实体、阈值配置和统一结果状态。
3. 先为领域模型和边界规则写测试。
4. 实现五表合成数据生成器和七个必造场景。
5. 实现 SQLAlchemy 模型、SQLite Repository 和数据质量检查。
6. 实现库存查询与最小 FastAPI 健康检查。
7. 运行完整测试并更新 README 的真实进度。

## 首批决策记录

| 决策 | 结论 | 原因 |
|---|---|---|
| 是否新建项目目录 | 否 | 现有库存归因项目与本需求同源 |
| 是否直接接入真实数据 | 否 | 合规、复现和可公开展示优先 |
| 是否让 LLM 计算指标 | 否 | 数值和规则必须确定、可测试 |
| 是否自动执行处置动作 | 否 | 写操作需要权限、审批和幂等设计 |
| 是否首期使用 Neo4j/NebulaGraph | 否 | NetworkX 足以验证图关系与路径策略 |
| 是否固定呆滞阈值 | 配置化 | 不同物料类别和企业口径可能不同 |
| 是否保留生产延期 | 是 | 与实习经历直接对应，能展示日期、状态、图路径和 Tool 证据闭环 |
| 是否实现完整生产模型 | 否 | MVP 只建简化生产单，不引入 BOM、工序、工作中心和领退料 |
| 是否保留销售取消与 ECN | 否 | 业务复杂度高于新增工程展示价值，移入扩展版本 |
