---
title: 智能库存归因 Agent 项目阶段任务清单
type: project-checklist
status: active
created: 2026-08-09
updated: 2026-08-10
project: inventory-root-cause-agent
repo_name: inventory-root-cause-agent
tags:
  - 项目管理
  - Agent
  - FastAPI
  - LangGraph
  - 库存归因
---

# 智能库存归因 Agent 项目阶段任务清单

> 项目业务名称：呆滞料智能体
> 正式代码仓库：`inventory-root-cause-agent`
> 使用原则：每完成并验证一项，就把 `- [ ]` 改为 `- [x]`；发现新任务时直接补到对应阶段，不另建零散清单。

## 0. 清单使用规则

- [x] 建立本阶段任务清单。
- [ ] 每次开工前查看“当前状态”和“下一项任务”。
- [ ] 每个任务完成后同时记录验证证据，不凭“代码写过了”直接打钩。
- [ ] 新发现的任务补充到对应阶段，并在更新记录中写明日期。
- [ ] 阶段退出条件全部满足后，再进入下一阶段。
- [ ] 每天结束前更新一次“下一项任务”和“阻塞问题”。
- [ ] 重要功能完成后提交 Git；避免把整阶段压成一个巨大提交。

任务完成证据可以是：测试名称、命令输出、接口响应、截图、评测报告或 Git Commit。

## 1. 当前状态看板

| 项目      | 当前状态                                                             |
| ------- | ---------------------------------------------------------------- |
| 当前日期    | 2026-08-10                                                       |
| 当前阶段    | Phase 1：领域模型与合成数据                                                |
| 当前主任务   | 分层骨架、六组领域枚举、阈值配置和首批单元测试已完成并通过质量门                               |
| 下一项编码任务 | 先写 `tests/unit/domain/test_entities.py` 中 `Material` 的默认值与非法成本用例 |
| 当前阻塞    | 无                                                                |
| 最近完成    | Phase 1 1.1～1.3：43 项测试通过，Ruff 检查通过                                |

## 2. 项目总路线

```text
Phase 0 需求、规则、环境和 Git
  → Phase 1 领域模型、合成数据、SQLite、确定性服务和基础 API
  → Phase 2 指标计算、三类根因、证据图和三个用例级 Agent Tools
  → Phase 3 Agent 工作流、上下文补全、校验、重试和降级
  → Phase 4 评测、Streamlit、性能验证、文档、录屏和简历
```

---

# Phase 0：需求冻结、环境准备与工程开工

## 0.1 项目目标与边界

- [x] 明确项目业务名称为“呆滞料智能体”。
- [x] 明确英文仓库名为 `inventory-root-cause-agent`。
- [x] 明确项目定位为个人作品集和企业 AI 应用工程实践。
- [x] 明确 MVP 用户：库存、采购、计划/生产和管理人员。
- [x] 明确最小分析粒度为 `material_id × warehouse_id × as_of_date`。
- [x] 明确只使用项目自行生成的合成数据。
- [x] 明确不接入真实 ERP、客户数据库或公司内部平台。
- [x] 明确不复制公司源码、真实表名、客户信息、凭据和内部地址。
- [x] 明确首期只读分析，不自动执行报废、调拨或订单修改。
- [x] 明确数值、规则命中和根因得分全部由确定性代码计算。
- [x] 明确 LLM 只负责理解问题、选择工具和组织回答。
- [x] 明确 LLM 不得改写指标、得分或证据。
- [x] 明确没有 LLM 密钥时，核心结构化分析仍然可运行。

参考：[[docs/00_项目章程]]、[[README]]。

## 0.2 业务口径与默认阈值

- [x] 定义当前库存、有效消耗、无消耗天数和平均日消耗。
- [x] 定义库存覆盖天数和呆滞金额。
- [x] 区分慢动、无动、过剩和报废/淘汰概念。
- [x] 文档化 `analysis_window_days = 90`。
- [x] 文档化 `slow_moving_days = 90`。
- [x] 文档化 `non_moving_days = 180`。
- [x] 文档化 `coverage_days_threshold = 120`。
- [x] 文档化 `demand_drop_ratio = 0.40`。
- [x] 文档化 `purchase_excess_days = 120`。
- [x] 文档化 `production_delay_days = 14`。
- [x] 明确接受以上默认值作为合成数据 MVP v0.2 口径。
- [x] 在文档中注明默认值不代表行业或企业统一标准。
- [x] 新物料保护期暂定为首次入库日起 30 天。
- [x] 单位成本 v0.2 统一采用合成标准成本和 `Decimal`。
- [x] 将其他待业务确认项标记为“MVP 默认”或“后续真实接入再确认”。

参考：[[docs/01_业务口径与需求]]。

## 0.3 数据和验收设计

- [x] 将 MVP 冻结为 Material、Warehouse、InventoryMovement、PurchaseOrder 和 ProductionOrder 五个持久化实体。
- [x] 明确库存从流水聚合，不建 InventorySnapshot。
- [x] 明确图边从外键派生，不建 SourceDocumentLink。
- [x] 明确销售单、ECN、Supplier 和完整生产模型移入扩展范围。
- [x] 明确三类根因：需求下降、超量采购、生产延期。
- [x] 明确四类图节点和三类受控关系。
- [x] 明确三个用例级 Agent Tool。
- [x] 定义受控库存移动类型。
- [x] 定义 `ok`、`empty`、`error`、`blocked` 四种数据状态。
- [x] 定义正常、单根因、多根因、空结果和坏数据必造场景。
- [x] 定义数据唯一性、引用完整性、数量、日期和对账质量规则。
- [x] 定义功能、测试、非功能和演示验收标准。
- [x] 明确首版不虚构测试覆盖率和性能指标。

参考：[[docs/02_数据准备清单]]、[[docs/03_验收标准]]。

## 0.4 Git 与仓库准备

- [x] 创建独立正式项目仓库 `inventory-root-cause-agent`。
- [x] 初始化独立 Git 仓库。
- [x] 使用 `main` 作为主分支。
- [x] 创建 `.gitignore`。
- [x] 创建 `.gitattributes`。
- [x] 确认 `.venv/` 被 Git 忽略。
- [x] 确认 `.env` 被 Git 忽略。
- [x] 创建首次提交 `chore: initialize inventory root-cause agent`。
- [x] 连接 GitHub 私有仓库 `origin`。
- [x] 推送并建立 `main → origin/main` 跟踪关系。
- [x] 确认 Git 工作区在环境配置前保持干净。
- [x] Phase 0 收尾提交前检查密钥、虚拟环境、缓存和合成数据体积。

## 0.5 Python 开发环境

- [x] 安装 Python 3.11.9。
- [x] 在项目根目录创建 `.venv`。
- [x] 激活 `.venv` 并确认 `python --version` 为 3.11.9。
- [x] 将 pip 升级到 26.2.1。
- [x] 确认 pip 路径位于项目 `.venv`。
- [x] 创建最小 Python 包 `app/__init__.py`。
- [x] 根据 `pyproject.toml` 安装正式依赖和 `[dev]` 依赖。
- [x] 运行 `python -m pip check`，无损坏依赖。
- [x] 成功导入 FastAPI、LangGraph、NetworkX、Pydantic、SQLAlchemy 和 Uvicorn。
- [x] 确认 pytest 9.1.1 可以运行。
- [x] 确认 ruff 0.16.2 可以运行。
- [ ] 确认 VS Code 状态栏选择的是项目 `.venv` 解释器。
- [x] 把完整依赖版本记录到 `requirements.lock`。

环境流程参考：[[Pyhon/Python项目前置工作]]。

## 0.6 Phase 0 退出验收

- [x] MVP 已收缩为五表、三根因、四节点、三边和三 Tool。
- [x] 生产延期与实习经历的映射、公开模型和防膨胀边界已写明。
- [x] 默认业务阈值和 MVP 默认口径已明确接受。
- [x] Python 3.11 隔离环境可用。
- [x] 正式依赖和开发工具可用。
- [x] 精确依赖版本已记录到 `requirements.lock`。
- [x] Git、远程仓库和敏感文件忽略规则可用。
- [x] Phase 1 任务已拆分并排序。
- [x] 更新代码仓库中的 `PREPARATION_CHECKLIST.md` 完成状态。
- [x] 更新代码仓库 `README.md`，将当前状态从 Phase 0 改为 Phase 1。
- [ ] 为 Phase 0 收尾创建一次小提交并推送。

---

# Phase 1：领域模型、合成数据、持久化、服务与基础 API

## 1.1 建立代码分层骨架

- [x] 创建 `app/__init__.py`。
- [x] 创建 `app/domain/__init__.py`。
- [x] 创建 `app/repositories/__init__.py`。
- [x] 创建 `app/services/__init__.py`。
- [x] 创建 `app/api/__init__.py`。
- [x] 创建 `tests/`。
- [x] 创建 `tests/unit/`。
- [x] 创建 `tests/integration/`。
- [x] 创建 `tests/e2e/`。
- [x] 确认 `app/domain` 不导入 FastAPI、SQLAlchemy 或具体数据库实现。
- [x] 确认公共函数和类方法使用类型注解。
- [x] 运行 `python -m ruff check .`，建立首个静态检查基线。

## 1.2 领域枚举与统一状态

- [x] 创建库存移动类型枚举 `MovementType`。
- [x] 创建生产订单状态枚举 `ProductionOrderStatus`。
- [x] 创建库存风险等级枚举 `InventoryRiskLevel`。
- [x] 创建根因类型枚举 `RootCauseType`。
- [x] 创建统一结果状态枚举 `ResultStatus`。
- [x] `ResultStatus` 包含 `ok`、`empty`、`error`、`blocked`。
- [x] 创建采购订单状态受控枚举，避免代码中散落字符串。
- [x] 为枚举值稳定性编写单元测试。
- [x] 为非法枚举输入编写拒绝测试。

## 1.3 业务阈值配置

- [x] 创建 `AnalysisThresholds` Pydantic 模型。
- [x] 将 90/180/120/40%/120/14 等默认值集中定义。
- [x] 禁止在计算函数内散落业务魔法数字。
- [x] 配置模型设置 `extra="forbid"`，拒绝未知字段。
- [x] 校验所有天数阈值为正数。
- [x] 校验 `non_moving_days >= slow_moving_days`。
- [x] 校验比例位于 0 到 1 之间。
- [x] 为默认值、边界值、非法值和未知字段编写测试。

## 1.4 核心领域实体

- [x] 实现 `Material`。
- [x] 校验 `material_id`、名称、类别、单位成本和创建日期。
- [x] 实现 `Warehouse`。
- [x] 实现 `InventoryMovement`。
- [x] 校验移动数量非零、日期存在、来源单据字段可选规则。
- [x] 实现 `PurchaseOrder`。
- [x] 校验收货量非负且业务状态有效。
- [x] 实现 `ProductionOrder`。
- [x] 校验计划消耗数量、计划/实际开工、到期、关闭日期和状态组合。
- [x] 明确 `ProductionOrder.material_id` 仅表示计划消耗的一种物料，不扩展 BOM。
- [x] 所有实体设置 `extra="forbid"`。
- [x] 所有金额和数量使用明确类型，避免浮点口径混乱。
- [x] 为每个实体编写正常、边界和非法输入测试。

## 1.5 领域结果模型

- [ ] 定义通用响应元数据：`status`、`trace_id`、`as_of_date`。
- [ ] 定义指标值、单位、观察窗口和完整性标记。
- [ ] 定义 `InventoryMetrics`。
- [ ] 定义 `RiskAssessment`。
- [ ] 定义 `EvidenceItem`。
- [ ] 定义 `RootCauseCandidate`。
- [ ] 定义 `AnalysisResult`。
- [ ] 明确 `empty` 响应不伪装为 `error`。
- [ ] 明确 `blocked` 响应保留事实但不输出业务结论。
- [ ] 单位成本缺失时允许数量结论，同时将金额标记为不完整。
- [ ] 为四种结果状态编写序列化测试。

## 1.6 合成数据生成器

- [ ] 创建合成数据模块和清晰的 `synthetic=true` 标记。
- [ ] 支持传入固定 `seed`。
- [ ] 支持记录 `dataset_version`。
- [ ] 支持记录 `generated_at`。
- [ ] 生成 15～20 个合成物料。
- [ ] 生成 2 个合成仓库。
- [ ] 生成 300～600 条库存流水、20～30 张采购单和 15～25 张生产单。
- [ ] 所有 ID 使用 `MAT-SYN-*`、`PO-SYN-*` 等明显前缀。
- [ ] 实现 `SYN-NORMAL-01`。
- [ ] 实现 `SYN-DEMAND-DROP-01`。
- [ ] 实现 `SYN-OVERBUY-01`。
- [ ] 实现 `SYN-PROD-DELAY-01`。
- [ ] 实现 `SYN-MULTI-CAUSE-01`。
- [ ] 实现 `SYN-EMPTY-01` 查询目标。
- [ ] 实现 `SYN-BLOCKED-01`。
- [ ] 相同版本和 seed 重复生成时业务事实一致。
- [ ] 测试断言依赖场景标签，不依赖随机碰巧生成的结果。

## 1.7 数据质量检查

- [ ] 检查主键唯一性。
- [ ] 检查外键和单据引用完整性。
- [ ] 检查枚举值和关系类型。
- [ ] 检查数量、金额和订单数量约束。
- [ ] 检查日期顺序和分析日期越界。
- [ ] 检查库存流水累计值与结构化查询结果一致。
- [ ] 检查图关系不存在悬空节点。
- [ ] 检查所有必造场景均存在。
- [ ] 检查每类根因至少有一个正例和一个反例。
- [ ] 扫描数据中是否出现真实 URL、凭据或疑似真实企业标识。
- [ ] 将质量问题转换为结构化 `blocked` 或 `error` 结果。
- [ ] 为每条质量规则编写通过和失败测试。

## 1.8 SQLAlchemy 与 SQLite Repository

- [ ] 设计 SQLAlchemy 数据表模型。
- [ ] 将领域模型与数据库模型分开。
- [ ] 创建 SQLite 数据库连接和 Session 管理。
- [ ] 决定 MVP 是否只用 `metadata.create_all`，并记录迁移策略。
- [ ] 定义 Repository 抽象接口。
- [ ] 实现 Material Repository。
- [ ] 实现 Warehouse Repository。
- [ ] 实现 InventoryMovement Repository。
- [ ] 实现 PurchaseOrder Repository。
- [ ] 实现 ProductionOrder Repository。
- [ ] 支持按物料、仓库和分析日期查询。
- [ ] 支持成功有数据结果。
- [ ] 支持成功无数据 `empty` 结果。
- [ ] 将数据库异常转换为 `error`，不伪装为空结果。
- [ ] 编写事务回滚测试。
- [ ] 编写 Repository 集成测试。
- [ ] 编写一条命令初始化数据库并写入固定 seed 数据。

## 1.9 确定性库存查询服务

- [ ] 创建应用服务接口，不直接依赖 FastAPI。
- [ ] 实现按物料与仓库读取库存事实。
- [ ] 实现按分析日期过滤未来记录。
- [ ] 实现按仓库和物料类别筛选。
- [ ] 实现风险清单按呆滞金额降序排序的基础结构。
- [ ] 每次分析显式传入 `as_of_date`。
- [ ] 禁止在领域逻辑中隐式使用当前系统时间。
- [ ] 为有数据、无数据、坏数据和数据库异常编写服务测试。

## 1.10 FastAPI 基础接口

- [ ] 创建 `app/api` 传输层。
- [ ] 创建 FastAPI 应用入口 `app/main.py`。
- [ ] 实现 `GET /health`。
- [ ] `/health` 返回应用状态和版本，不泄露密钥和内部路径。
- [ ] 实现结构化库存查询入口。
- [ ] 请求模型拒绝未知字段。
- [ ] 所有响应包含 `trace_id`。
- [ ] 映射 `ok`、`empty`、`error`、`blocked` 到稳定响应契约。
- [ ] 为成功、参数错误、空结果、质量阻断和内部错误编写 API 测试。
- [ ] 使用 Uvicorn 本地启动并完成一次手工请求验证。

## 1.11 Phase 1 退出验收

- [ ] 固定 seed 能重复生成相同合成事实。
- [ ] SQLite 能保存和查询合成数据。
- [ ] Repository 测试覆盖有数据、无数据和异常。
- [ ] 基础库存查询服务不依赖 LLM。
- [ ] `/health` 和结构化查询接口通过测试。
- [ ] `python -m pytest` 全部通过。
- [ ] `python -m ruff check .` 全部通过。
- [ ] 更新 README 的启动、生成数据和测试命令。
- [ ] 提交并推送 Phase 1 可运行基线。

---

# Phase 2：指标计算、三类根因、证据图与 Agent Tools

## 2.1 库存指标计算

- [ ] 计算分析日期的当前库存。
- [ ] 识别最后一次有效消耗日期。
- [ ] 计算无消耗天数。
- [ ] 计算观察窗口内有效消耗量。
- [ ] 计算平均日消耗。
- [ ] 计算库存覆盖天数。
- [ ] 平均日消耗为 0 时返回 `coverage_days=null`。
- [ ] 平均日消耗为 0 时设置 `infinite_coverage=true`。
- [ ] 计算呆滞数量和呆滞金额。
- [ ] 单位成本缺失时金额返回 `null` 并标记不完整。
- [ ] 当前库存为 0 时不判为当前呆滞库存。
- [ ] 当前库存为负时返回质量阻断。
- [ ] 无消耗且首次入库缺失时返回质量阻断。
- [ ] 最近消耗晚于分析日期时返回质量阻断。
- [ ] 分析窗口不足时设置 `partial_window=true`。
- [ ] 调拨记录不重置有效消耗日期。
- [ ] 新物料保护期生效。
- [ ] 所有指标返回值、单位、窗口和计算日期。
- [ ] 为正常、临界值、零消耗、缺失和非法输入编写测试。

## 2.2 风险分级

- [ ] 实现 `NORMAL` 判定。
- [ ] 实现 `SLOW_MOVING` 判定。
- [ ] 实现 `NON_MOVING` 判定。
- [ ] 实现 `DATA_QUALITY_BLOCKED` 判定。
- [ ] 验证 89/90 天临界值。
- [ ] 验证 179/180 天临界值。
- [ ] 验证 119/120 天覆盖临界值。
- [ ] 相同数据和分析日期重复执行结果一致。

## 2.3 三类确定性根因规则

### 需求下降

- [ ] 计算相邻观察窗口的有效需求或消耗。
- [ ] 实现下降比例阈值命中。
- [ ] 输出规则 ID、命中条件、得分和证据。
- [ ] 编写命中、不命中和证据不足测试。

### 超量采购

- [ ] 计算到货量、剩余量和采购形成的覆盖天数。
- [ ] 实现采购覆盖过高规则。
- [ ] 输出关联采购单证据。
- [ ] 编写命中、不命中和证据不足测试。

### 生产延期

- [ ] 计算计划日期、实际日期和延期天数。
- [ ] 排除已正确关闭且无延期的生产单。
- [ ] 实现超过 14 天的默认延期规则。
- [ ] 校验计划消耗数量大于 0 且当前库存大于 0。
- [ ] 输出生产单状态、计划/实际开工日、延期天数和计划消耗数量证据。
- [ ] 编写命中、不命中和证据不足测试。

### 多根因排序

- [ ] 允许多个候选根因同时存在。
- [ ] 按确定性得分稳定排序。
- [ ] 定义同分时固定排序规则。
- [ ] 明确根因分数不是统计概率。
- [ ] 证据不足时显示 `insufficient_evidence`。
- [ ] 验证 `SYN-MULTI-CAUSE-01` 至少命中两类根因。

## 2.4 NetworkX 关系图

- [ ] 定义物料、仓库、采购单和生产单四类节点。
- [ ] 定义 `PURCHASES`、`CONSUMES`、`STORED_IN` 三类受控边。
- [ ] 从业务外键构建 NetworkX 图，不维护通用关系表。
- [ ] 每个节点保留来源和合成标记。
- [ ] 每条边保留关系类型和来源标识。
- [ ] 实现按物料查询关联路径。
- [ ] 限制最大跳数。
- [ ] 限制最大节点数。
- [ ] 限制查询执行时间。
- [ ] 无关系路径时返回 `empty`。
- [ ] 悬空节点触发质量检查。
- [ ] 为单路径、多路径、空路径和限制触发编写测试。

## 2.5 Agent Tools

- [ ] 创建 `app/tools/__init__.py`。
- [ ] 为工具输入定义严格 Pydantic Schema。
- [ ] 实现 `list_inventory_risks` Tool。
- [ ] 实现 `analyze_material_root_cause` Tool。
- [ ] 实现 `trace_evidence` Tool。
- [ ] 指标计算和单条根因规则只作为内部 Service，不重复注册 Tool。
- [ ] Tool 返回结构化结果，不返回自由文本事实。
- [ ] Tool 统一返回 `ok`、`empty`、`error` 或 `blocked`。
- [ ] Tool 输出包含证据引用和 `trace_id`。
- [ ] Tool 不暴露数据库连接和内部异常堆栈。
- [ ] 为参数错误、空结果、依赖失败和正常结果编写测试。

## 2.6 Phase 2 退出验收

- [ ] 所有指标计算和边界规则通过测试。
- [ ] 三类根因各有正例、反例和证据不足用例。
- [ ] 多根因结果稳定排序。
- [ ] 图查询路径可追溯且受限制。
- [ ] 所有 Tool 契约稳定并通过测试。
- [ ] 不使用 LLM 也能得到完整结构化归因结果。
- [ ] `python -m pytest` 和 `python -m ruff check .` 全部通过。
- [ ] 提交并推送 Phase 2 确定性分析基线。

---

# Phase 3：LangGraph Agent、校验、重试与降级

## 3.1 LLM 接入准备

- [ ] 决定首版使用的 OpenAI 兼容模型服务。
- [ ] 在 `pyproject.toml` 中补充所需 SDK 依赖。
- [ ] 在 `.env.example` 中添加变量名，不写真实密钥。
- [ ] 在本地 `.env` 配置密钥，并确认 `.env` 被 Git 忽略。
- [ ] 使用 Pydantic Settings 读取模型配置。
- [ ] 配置请求超时、最大重试次数和模型名称。
- [ ] 为系统提示词和 Tool 描述定义版本号，并在 Trace 中记录版本。
- [ ] 日志中不记录密钥和完整敏感输入。
- [ ] 无密钥时应用能够启动并进入降级模式。

## 3.2 Agent 状态与节点

- [ ] 创建 `app/agent/__init__.py`。
- [ ] 定义 Agent State。
- [ ] 定义用户问题、结构化参数、工具结果、证据和最终响应字段。
- [ ] 实现输入解析节点。
- [ ] 提取物料、仓库、分析日期和分析意图。
- [ ] 定义最小会话上下文，只保存已确认的物料、仓库、分析日期和意图。
- [ ] 实现可替换的内存会话存储，并配置 TTL、最大轮数和清理策略。
- [ ] 参数缺失时只询问必要信息。
- [ ] 禁止猜测不存在的物料 ID。
- [ ] 实现工具选择节点。
- [ ] 实现工具执行节点。
- [ ] 实现证据校验节点。
- [ ] 实现结果组织节点。
- [ ] 实现模板降级节点。
- [ ] 编译 LangGraph 工作流。

## 3.3 校验和反思机制

- [ ] 检查工具返回是否为空。
- [ ] 检查工具返回是否为错误。
- [ ] 检查业务结论是否有证据支撑。
- [ ] 检查 LLM 摘要中的数字是否与工具结果一致。
- [ ] 发现数字不一致时丢弃 LLM 数字并使用结构化值。
- [ ] 空结果时允许换一次合理查询策略。
- [ ] 参数本身缺失时停止重试并请求用户补充。
- [ ] 数据质量阻断时禁止强行归因。
- [ ] 设置最大重试次数，防止无限循环。
- [ ] 设置最大 Agent 执行步数，达到上限后安全降级。
- [ ] 记录工具和动作摘要，不记录或暴露私有思维链。

## 3.4 自然语言接口

- [ ] 实现自然语言分析入口，例如 `POST /chat`。
- [ ] 实现工具列表入口，例如 `GET /tools`。
- [ ] 返回结构化结论、指标、候选根因、证据和建议。
- [ ] 所有自然语言响应包含 `trace_id`。
- [ ] LLM 不可用时返回结构化结果或模板摘要。
- [ ] 区分用户参数错误、业务空结果、质量阻断和系统异常。
- [ ] 接口不返回 Chain-of-Thought。
- [ ] 编写 API 契约测试。

## 3.5 Agent 测试

- [ ] 使用 mock 模型测试，不让单元测试依赖外网。
- [ ] 测试正确工具选择。
- [ ] 测试参数抽取。
- [ ] 测试缺失参数追问。
- [ ] 测试两轮对话补全仓库或分析日期。
- [ ] 测试会话过期后不复用旧参数。
- [ ] 测试空结果换策略。
- [ ] 测试数据库错误不被当成空结果。
- [ ] 测试证据不足不强行归因。
- [ ] 测试 LLM 数字与工具不一致时以工具为准。
- [ ] 测试无 LLM 密钥降级。
- [ ] 测试最大重试次数。
- [ ] 测试响应不泄露密钥、异常堆栈和私有推理。

## 3.6 Phase 3 退出验收

- [ ] 自然语言问题能够正确提取关键参数。
- [ ] Agent 能正确选择并调用确定性工具。
- [ ] 结论引用的指标和证据可追溯。
- [ ] 空结果、错误和质量阻断行为正确。
- [ ] 无 LLM 时核心分析仍然可用。
- [ ] 同一数据库和分析日期的结构化结果保持一致。
- [ ] 测试和静态检查全部通过。
- [ ] 提交并推送 Phase 3 Agent 基线。

---

# Phase 4：评测、演示页面、性能、文档与求职交付

## 4.1 Agent 评测集

- [ ] 设计至少 12 条 Agent 评测样例。
- [ ] 覆盖正常物料查询。
- [ ] 覆盖三类单根因查询。
- [ ] 覆盖多根因查询。
- [ ] 覆盖参数缺失和参数错误。
- [ ] 覆盖两轮参数补全和会话过期。
- [ ] 覆盖无数据、坏数据和依赖失败。
- [ ] 覆盖无 LLM 降级。
- [ ] 定义工具选择准确性指标。
- [ ] 定义参数抽取准确性指标。
- [ ] 定义任务完成指标。
- [ ] 定义证据引用完整性指标。
- [ ] 定义安全降级通过率。
- [ ] 编写一条可重复执行的评测命令。
- [ ] 保存评测结果和运行环境版本。
- [ ] 简历只写可由评测命令复现的数字。

## 4.2 Streamlit 演示页面

- [ ] 安装并记录 `[ui]` 依赖。
- [ ] 创建 `app/ui` 或独立演示入口。
- [ ] 实现聊天输入框。
- [ ] 实现物料、仓库和分析日期筛选。
- [ ] 展示风险等级和关键指标。
- [ ] 展示候选根因稳定排序。
- [ ] 展示证据单据和关系路径。
- [ ] 侧边栏只显示 Tool/Action 摘要，不显示私有思维链。
- [ ] 明确标注所有数据均为合成数据。
- [ ] 展示 `empty`、`blocked` 和 LLM 降级状态。
- [ ] 完成正常、多根因和降级场景手工验收。

## 4.3 日志、可观测性与安全检查

- [ ] 统一日志格式并带 `trace_id`。
- [ ] 记录接口耗时、工具名称、状态和错误类别。
- [ ] 不记录 API Key。
- [ ] 不记录完整敏感输入。
- [ ] 不记录 Chain-of-Thought。
- [ ] 对异常返回稳定错误码和用户可理解的信息。
- [ ] 扫描 Git 跟踪文件中的密钥和真实企业标识。
- [ ] 确认只有 `.env.example` 被提交。

## 4.4 性能与可靠性验证

- [ ] 固定一份本地基线数据集。
- [ ] 测量单次结构化分析耗时。
- [ ] 验证本地目标是否达到 2 秒以内。
- [ ] 记录电脑配置、数据规模和测量方法。
- [ ] 不达标时定位数据库、图查询或 Agent 调用耗时。
- [ ] 对图查询验证最大跳数、节点数和超时限制。
- [ ] 连续重复执行验证结果稳定。
- [ ] 记录真实性能数据，不编造提升百分比。

## 4.5 自动化质量门

- [ ] `python -m pytest` 全部通过。
- [ ] `python -m pytest --cov=app` 生成真实覆盖率报告。
- [ ] 根据真实基线决定覆盖率门槛。
- [ ] `python -m ruff check .` 全部通过。
- [ ] 新建全新 `.venv` 完成一次冷启动复现。
- [ ] 按 README 从零执行安装、生成数据、启动和测试命令。
- [ ] 确认没有依赖开发者电脑的绝对路径。
- [ ] 确认无 LLM 配置时结构化链路仍可运行。

## 4.6 README 与项目文档

- [ ] README 写明项目要解决的业务问题。
- [ ] README 写明仅使用合成数据。
- [ ] README 添加清晰架构图。
- [ ] README 添加目录结构说明。
- [ ] README 添加 Python 版本和环境安装步骤。
- [ ] README 添加初始化数据库和生成种子数据命令。
- [ ] README 添加启动 FastAPI 命令。
- [ ] README 添加启动 Streamlit 命令。
- [ ] README 添加测试、静态检查和评测命令。
- [ ] README 添加三个可演示问题。
- [ ] README 说明 `ok`、`empty`、`error`、`blocked`。
- [ ] README 说明无 LLM 降级行为。
- [ ] 更新项目学习与面试地图。
- [ ] 新建或更新“项目话术底稿”。

## 4.7 两分钟演示录屏

- [ ] 编写两分钟演示脚本。
- [ ] 0～20 秒介绍业务痛点和项目目标。
- [ ] 20～50 秒展示正常物料。
- [ ] 50～100 秒展示多根因呆滞物料和证据链。
- [ ] 100～115 秒展示无数据或 LLM 不可用降级。
- [ ] 115～120 秒展示测试与评测结果。
- [ ] 录屏前关闭真实通知和无关窗口。
- [ ] 确认画面中没有 API Key、私人信息和内部地址。
- [ ] 录制并检查声音、画面、字幕和总时长。
- [ ] 保存最终视频和一份备用副本。

## 4.8 简历与面试材料

- [ ] 用一句话说明项目业务价值。
- [ ] 用一条描述确定性指标与规则归因。
- [ ] 用一条描述 LangGraph 工具编排、校验和降级。
- [ ] 用一条描述测试、评测或性能的真实量化结果。
- [ ] 准备“为什么不用 LLM 直接算指标”的回答。
- [ ] 准备“如何区分 empty 和 error”的回答。
- [ ] 准备“如何防止 LLM 幻觉修改业务事实”的回答。
- [ ] 准备“为什么首期用 NetworkX 而不是 Neo4j”的回答。
- [ ] 准备“如何从合成数据迁移到真实 ERP”的回答。
- [ ] 用 STAR 法完成三遍口述演练。

## 4.9 项目交付与版本管理

- [ ] 清理未使用依赖和临时代码。
- [ ] 检查 Git 工作区无意外文件。
- [ ] 推送最终代码到 GitHub 私有仓库。
- [ ] 创建可识别的 MVP 版本标签。
- [ ] 在项目总索引中把“可运行”更新为已验证。
- [ ] 记录验证日期、Python 版本和核心命令。
- [ ] 将项目状态从 Active 更新为 Portfolio 或 Completed。

## 4.10 Phase 4 最终退出验收

- [ ] 两分钟内展示正常物料、多根因物料和降级场景。
- [ ] 新环境可以按 README 完整复现。
- [ ] 所有自动化测试和评测命令可运行。
- [ ] 简历中的每个技术指标都有可复现证据。
- [ ] 仓库中不存在真实数据、密钥和公司内部信息。
- [ ] README、演示视频、简历话术和 Git 版本全部完成。

---

# 持续维护任务

## 每次编码前

- [ ] 激活 `.venv`。
- [ ] 确认当前分支和 `git status`。
- [ ] 从本清单选择一个足够小的任务。
- [ ] 先明确输入、输出、异常和验收测试。

## 每次编码后

- [ ] 运行相关单元测试。
- [ ] 运行 `python -m ruff check .`。
- [ ] 检查没有写入真实数据、密钥和绝对路径。
- [ ] 更新本清单的完成状态。
- [ ] 记录下一项最小任务。
- [ ] 用清晰的提交信息提交 Git。

## 每个阶段结束后

- [ ] 运行完整测试。
- [ ] 更新 README 真实进度。
- [ ] 更新 Obsidian 项目笔记和面试话术。
- [ ] 推送远程仓库。
- [ ] 复盘本阶段最大问题和下一阶段风险。

---

# 更新记录

| 日期 | 更新内容 | 下一项任务 |
|---|---|---|
| 2026-08-09 | 创建详细阶段任务清单；同步 Git、Python 3.11、`.venv` 和依赖验证进度 | 接受默认阈值，建立 Phase 1 分层目录 |
| 2026-08-09 | 按 AI 应用工程师岗位重新收缩范围；保留生产延期，删除销售取消、ECN、快照和通用关系表 | 逐项复核 Phase 0 未决口径 |
| 2026-08-10 | 完成 Phase 1 1.1～1.3：分层骨架、六组枚举、`AnalysisThresholds` 与 43 项测试；Ruff 通过 | 测试先行实现 `Material` 等五个领域实体 |
| 2026-08-11 | 完成 Phase 1 1.4：五个领域实体、公共导出与 42 项实体测试；全量 85 项测试和 Ruff 通过 | 测试先行实现固定 seed 合成数据与质量检查 |
