---
title: 智能库存归因 Agent 项目复盘与成果证据
type: project-evidence
status: active
created: 2026-08-09
updated: 2026-08-12
project: inventory-root-cause-agent
---

# 智能库存归因 Agent：项目复盘与成果证据

> 本文件只记录已经完成并可复现的事实。计划中的指标保留在 [[项目阶段任务清单]]，不提前写成成果。

## 1. 当前完成范围

- 已完成：Phase 1 本地可运行基线，包括领域模型、固定 seed 合成数据、质量检查、SQLite/SQLAlchemy Repository、确定性库存服务、风险清单和 FastAPI 基础接口。
- 未完成：Phase 1 基线提交推送；Phase 2 根因评分、证据图、Agent Tool，以及后续 Agent 评测、性能报告和演示交付。

## 2. 阶段验收证据

| 阶段 | 验收状态 | 证据位置 | 验证命令/方式 | 日期 |
|---|---|---|---|---|
| Phase 0 | 已完成 | [[PREPARATION_CHECKLIST]]、`requirements.lock` | `pip check`、导入检查、`ruff check . --no-cache`、安全扫描 | 2026-08-09 |
| Phase 1 | 本地退出验收通过；待提交推送 | `app/`、`scripts/`、`tests/` | 151 项 pytest、Ruff、真实 SQLite 初始化、Uvicorn 请求 | 2026-08-12 |
| Phase 2 | 未开始 | - | - | - |
| Phase 3 | 未开始 | - | - | - |
| Phase 4 | 未开始 | - | - | - |

## 3. 测试与评测结果

| 指标 | 结果 | 数据集/报告 | 复现命令 | Commit |
|---|---:|---|---|---|
| 依赖健康 | 通过，无损坏依赖 | `requirements.lock` | `python -m pip check` | 待 Phase 0 收尾提交 |
| 锁文件一致性 | 通过，与已验证环境完全一致 | `requirements.lock` | `pip freeze --exclude-editable` 后比较 | 待 Phase 0 收尾提交 |
| 静态检查 | 通过 | 全仓库 | `python -m ruff check . --no-cache` | 待 Phase 0 收尾提交 |
| 单元测试 | 43 项通过 | 领域枚举与 `AnalysisThresholds` | `python -m pytest -p no:cacheprovider` | `c49d243` |
| Phase 1 静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check . --no-cache` | `c49d243` |
| 领域实体定向测试 | 42 项通过 | 五个 Pydantic 领域实体 | `python -m pytest tests/unit/domain/test_entities.py -p no:cacheprovider` | `082b1d9` |
| Phase 1 当前全量测试 | 85 项通过 | 枚举、阈值与五个领域实体 | `python -m pytest -p no:cacheprovider` | `082b1d9` |
| Phase 1 当前静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check .` | `082b1d9` |
| Phase 1 最终全量测试 | 151 项通过；1 条第三方 TestClient 弃用提示 | 领域、数据、Repository、服务与 API | `python -m pytest -p no:cacheprovider` | 待提交 |
| Phase 1 最终静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check .` | 待提交 |
| 固定 seed 数据库初始化 | 18 物料、2 仓库、360 流水、24 采购单、18 生产单 | `data/generated/phase1_verification.db`（忽略生成物） | `python scripts/init_db.py --database data/generated/phase1_verification.db --seed 20260812` | 待提交 |
| 真实 Uvicorn 请求 | `/health=ok`；结构化分析 `ok/NORMAL` 且含 trace_id | 本机 `127.0.0.1:8765` 临时验证 | `python -m uvicorn app.main:app` 后请求接口 | 待提交 |
| 安全扫描 | 通过；未发现密钥形态、真实数据或本机绝对路径 | 20 个非环境文件 | 提交前关键字与凭据格式扫描 | 待 Phase 0 收尾提交 |
| Agent 任务完成率 | 待验证 | - | 待实现 | - |
| 证据引用正确率 | 待验证 | - | 待实现 | - |

## 4. 演示证据

- 正常链路：待实现。
- 空结果或降级链路：待实现。
- 演示入口与录屏：待实现。

## 5. 关键决策与权衡

- 确定性计算与规则归因负责数值和结论依据，LLM 只负责意图理解、工具编排和语言组织。
- 第一版使用纯合成数据，避免任何公司数据、代码、真实表名和内部信息进入个人项目。
- 第一版图关系适配器使用 NetworkX，先验证业务链路，再考虑外部图数据库。
- 生产延期因与实习经历直接相关而保留，但使用单物料计划消耗的简化生产单模型，不扩展 BOM、工序和领退料。
- 销售取消、ECN、库存快照和通用来源关系表移入扩展范围，以工程闭环优先于业务实体数量。

## 6. 项目问题与复盘

- 合成填充流水最初独立随机正负，导致普通物料出现非预期负库存；质量检查捕获后改为“无库存先入库、出库不超过余额”，只保留显式阻断场景。
- 结果模型最初把 `complete=true` 错误等同于“必须有数值”；按业务口径修正为零消耗时覆盖天数可以为 `null`，同时 `infinite_coverage=true` 且事实完整。
- FastAPI 生命周期测试最初未使用 `TestClient` 上下文，导致引擎未初始化；改为按真实启动方式进入 lifespan。
- 风险清单最初会把 Repository 异常吞成空列表；改为领域级 `RiskListResult`，严格区分 `ok`、`empty` 与 `error`。

## 7. 简历与面试表述

只在对应证据产生后填写：

- 一句话项目价值：待验证后定稿。
- 个人职责：待开发后定稿。
- 量化成果：待测试和评测后填写。
- 最有价值的技术权衡：待阶段复盘后填写。
- 最困难问题及解决过程：待真实发生后填写。
