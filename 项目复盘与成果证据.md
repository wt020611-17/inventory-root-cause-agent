---
title: 智能库存归因 Agent 项目复盘与成果证据
type: project-evidence
status: portfolio
created: 2026-08-09
updated: 2026-08-16
project: inventory-root-cause-agent
---

# 智能库存归因 Agent：项目复盘与成果证据

> 本文件只记录已经完成并可复现的事实。计划中的指标保留在 [[项目阶段任务清单]]，不提前写成成果。

## 1. 当前完成范围

- 已完成：Phase 1 可运行后端基线；Phase 2 指标边界、三类确定性根因、受限 NetworkX 证据图和三个强类型 Agent Tool。
- 已完成：Phase 3，包括 DeepSeek 适配器、完整 LangGraph 工作流、会话补参、证据校验、降级、自然语言 API 和真实调用验收。
- 已完成：Phase 4.1 Agent 评测集、五项指标、可复现运行器与首份离线基线。
- 已完成：Phase 4.2 Streamlit 演示页、纯展示转换层、正常/多根因/无 LLM 交互验收。
- 已完成：Phase 4.3 统一 JSON 审计日志、API/Tool 可观测字段、稳定错误响应和仓库安全扫描。
- 已完成：Phase 4.4 固定 seed 三层性能基线、图限制与连续稳定性验证。
- 已完成：Phase 4.5 真实覆盖率门槛、全新环境冷启动、无 LLM 入口和绝对路径依赖验证。
- 已完成：Phase 4.6 对外 README、项目学习与面试地图和项目话术底稿。
- 已完成：Phase 4.7 自动化演示验收；用户决定不录屏，正常、多根因、证据路径和空结果 4/4 通过。
- 已完成：Phase 4.8 可投递简历描述、五个高频问答和三轮 STAR 书面口述模拟。
- 已完成：Phase 4.9/4.10 最终依赖审计、质量门、Git 交付与 `v0.1.0` 发布；项目进入 Portfolio 维护状态。

## 2. 阶段验收证据

| 阶段 | 验收状态 | 证据位置 | 验证命令/方式 | 日期 |
|---|---|---|---|---|
| Phase 0 | 已完成 | [[PREPARATION_CHECKLIST]]、`requirements.lock` | `pip check`、导入检查、`ruff check . --no-cache`、安全扫描 | 2026-08-09 |
| Phase 1 | 已完成并推送 | `app/`、`scripts/`、`tests/` | 151 项 pytest、Ruff、真实 SQLite 初始化、Uvicorn 请求 | 2026-08-12 |
| Phase 2 | 已完成并推送 | `app/services/`、`app/tools/`、`tests/unit/` | 186 项 pytest、Ruff | 2026-08-13 |
| Phase 3 | 已完成；工程基线已推送（`c429aa5`） | `app/agent/`、`tests/unit/agent/`、`tests/integration/test_api.py` | 207 项 pytest、Ruff、pip check、真实 DeepSeek 调用 | 2026-08-14 |
| Phase 4 | 已完成并发布 `v0.1.0` | `evals/`、`app/ui/`、`app/core/`、`benchmarks/`、`reports/`、`README.md` 与求职材料 | 227 项 pytest、91.27% 分支覆盖率、Ruff、pip check、安全扫描、冷启动、评测、性能、文档、自动演示、求职材料与 Git 交付复核 | 2026-08-16 |

## 3. 测试与评测结果

| 指标 | 结果 | 数据集/报告 | 复现命令 | Commit |
|---|---:|---|---|---|
| 依赖健康 | 通过，无损坏依赖 | `requirements.lock` | `python -m pip check` | 待 Phase 0 收尾提交 |
| 锁文件一致性 | 通过，与已验证环境完全一致 | `requirements.lock` | `pip freeze --exclude-editable` 后比较 | 待 Phase 0 收尾提交 |
| 静态检查 | 通过 | 全仓库 | `python -m ruff check . --no-cache` | 待 Phase 0 收尾提交 |
| 单元测试 | 43 项通过 | 领域枚举与 `AnalysisThresholds` | `python -m pytest -p no:cacheprovider` | `c49d243` |
| Phase 1 静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check . --no-cache` | `c49d243` |
| 领域实体定向测试 | 42 项通过 | 五个 Pydantic 领域实体 | `python -m pytest tests/unit/domain/test_entities.py -p no:cacheprovider` | `082b1d9` |
| Phase 1 当前全量测试 | 85 项通过 | 枚举、阈值与五个领域实体 | `python -m pytest -p no:cacheprovider` | `082b1d9` |
| Phase 1 当前静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check .` | `082b1d9` |
| Phase 1 最终全量测试 | 151 项通过；1 条第三方 TestClient 弃用提示 | 领域、数据、Repository、服务与 API | `python -m pytest -p no:cacheprovider` | `c78525e` |
| Phase 1 最终静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check .` | `c78525e` |
| Phase 2 最终全量测试 | 186 项通过；1 条第三方 TestClient 弃用提示 | 指标、根因、图、Tool 与 Phase 1 回归 | `python -m pytest -p no:cacheprovider` | `51e424f` |
| Phase 2 最终静态检查 | 通过，`All checks passed!` | 全仓库 | `python -m ruff check . --no-cache` | `51e424f` |
| Phase 3 最终无密钥回归 | 207 项通过；1 条第三方弃用提示；Ruff、pip check 通过 | DeepSeek 适配、完整 Agent 工作流、API 与降级链路 | `python -m pytest -p no:cacheprovider` | `c429aa5` |
| Phase 3 DeepSeek 真实验收 | 参数解析完整；Agent `ok`；正确调用根因工具；2 条证据；无密钥或私有推理泄露 | 固定 seed 合成数据与本地 `.env` | DeepSeek 最小解析调用 + 完整 `invoke_agent` 调用 | `c429aa5` |
| 固定 seed 数据库初始化 | 18 物料、2 仓库、360 流水、24 采购单、18 生产单 | `data/generated/phase1_verification.db`（忽略生成物） | `python scripts/init_db.py --database data/generated/phase1_verification.db --seed 20260812` | `c78525e` |
| 真实 Uvicorn 请求 | `/health=ok`；结构化分析 `ok/NORMAL` 且含 trace_id | 本机 `127.0.0.1:8765` 临时验证 | `python -m uvicorn app.main:app` 后请求接口 | `c78525e` |
| Phase 4.3 安全扫描 | 通过；0 个问题；Git 仅跟踪 `.env.example` | Git 已跟踪及未忽略文件 | `python scripts/security_scan.py` | `4c29fa8` / `v0.1.0` |
| Agent 工具选择准确率 | 12/12（100%） | `evals/datasets/agent_eval_v1.json`、`evals/results/agent_eval_v1_baseline.json` | `python -m evals.runners.agent_eval` | `4c29fa8` / `v0.1.0` |
| Agent 参数抽取准确率 | 15/15（100%） | 同上 | `python -m evals.runners.agent_eval` | `4c29fa8` / `v0.1.0` |
| Agent 任务完成率 | 15/15（100%） | 同上 | `python -m evals.runners.agent_eval` | `4c29fa8` / `v0.1.0` |
| Agent 证据引用完整性 | 8/8（100%） | 同上 | `python -m evals.runners.agent_eval` | `4c29fa8` / `v0.1.0` |
| Agent 安全降级通过率 | 5/5（100%） | 同上 | `python -m evals.runners.agent_eval` | `4c29fa8` / `v0.1.0` |
| Phase 4.2 UI 验收 | 6 项通过；正常、多根因、证据路径、empty、blocked、无 LLM 状态可展示 | `app/ui/`、`tests/unit/ui/` | `python -m pytest tests/unit/ui -p no:cacheprovider` | `4c29fa8` / `v0.1.0` |
| Phase 4.3 可观测性验收 | 8 项通过；Trace ID、接口耗时、工具、状态、错误类别、字段白名单、稳定 422/500 | `app/core/`、`tests/integration/test_observability.py`、安全扫描测试 | 定向 pytest + 全量 pytest | `4c29fa8` / `v0.1.0` |
| 结构化归因性能 | 30 次：中位数 1.876 ms，P95 3.233 ms，最大 3.344 ms | `benchmarks/results/performance_baseline.json` | `python -m benchmarks.run_performance` | `4c29fa8` / `v0.1.0` |
| 证据图性能 | 30 次：中位数 1.948 ms，P95 2.921 ms，最大 3.263 ms | 同上 | 同上 | `4c29fa8` / `v0.1.0` |
| 无 LLM Agent 性能 | 30 次：中位数 27.743 ms，P95 30.680 ms，最大 77.630 ms | 同上 | 同上 | `4c29fa8` / `v0.1.0` |
| 性能可靠性 | 三条链路重复结果稳定；2 跳、50 节点、100 ms 图限制全部验证通过 | 同上、`tests/benchmarks/` | 性能命令 + 定向 pytest | `4c29fa8` / `v0.1.0` |
| 分支覆盖率 | 91.27%，90% 门槛通过 | `reports/coverage.json`、`pyproject.toml` | `python -m pytest -p no:cacheprovider --cov=app --cov-report=term-missing --cov-report=json:reports/coverage.json` | `4c29fa8` / `v0.1.0` |
| Phase 4.5 冷启动 | Python 3.11.9 全新环境完成安装、数据初始化、226 项测试、API/Streamlit 健康检查与无 LLM 请求；临时环境已清理 | `reports/cold_start_verification.json` | 按 README 从零复现 | `4c29fa8` / `v0.1.0` |
| Phase 4.6 文档验收 | README 覆盖业务、合成数据、架构、目录、运行、三个演示问题、四状态和无 LLM 降级；学习地图与话术同步 | `README.md`、`项目学习与面试地图.md`、`项目话术底稿.md` | Markdown 链接、关键字段与安全扫描复核 | `4c29fa8` / `v0.1.0` |
| Phase 4.7 自动演示 | 4/4 通过：正常 `ok` 无确认根因；多根因命中超量采购与生产延期并引用 2 条证据；证据追踪返回 3 条路径；空结果为 `empty`；全部 `llm_used=false` | `reports/demo_verification.json`、`tests/e2e/test_demo_verification.py` | `python -m scripts.run_demo_verification`、227 项 pytest | `4c29fa8` / `v0.1.0` |
| Phase 4.8 求职材料 | 一句话业务价值、三条简历 bullet、五个高频问答、45/90/120 秒 STAR 三轮书面模拟均只引用可复现事实 | `简历与面试材料.md`、`项目话术底稿.md` | 对照评测、性能、覆盖率和自动演示报告逐项核对 | `4c29fa8` / `v0.1.0` |
| Phase 4.9/4.10 最终交付 | 最终质量门全部通过；功能提交、远程合并与版本标签均可核验 | `reports/final_verification.json`、Git 历史与远程标签 | 最终质量命令 + `git status` + 远程引用核对 | `4c29fa8` / `aaa3d28` / `v0.1.0` |

## 4. 演示证据

- 正常链路：Streamlit 可展示正常物料、4 个核心指标和 `ok` 状态。
- 多根因链路：可展示“超量采购 → 生产延期”稳定排序、证据单据和关系路径。
- 空结果或降级链路：可区分 `empty`、`blocked`，并明确展示确定性模板降级。
- 交互演示入口：`python -m streamlit run app/ui/streamlit_app.py`。
- 自动演示入口：`python -m scripts.run_demo_verification`；用户决定以结构化自动验收替代录屏。

## 5. 关键决策与权衡

- 确定性计算与规则归因负责数值和结论依据，LLM 只负责意图理解、工具编排和语言组织。
- 第一版使用纯合成数据，避免任何公司数据、代码、真实表名和内部信息进入个人项目。
- 第一版图关系适配器使用 NetworkX，先验证业务链路，再考虑外部图数据库。
- 生产延期因与实习经历直接相关而保留，但使用单物料计划消耗的简化生产单模型，不扩展 BOM、工序和领退料。
- 销售取消、ECN、库存快照和通用来源关系表移入扩展范围，以工程闭环优先于业务实体数量。

## 6. 项目问题与复盘

- 合成填充流水最初独立随机正负，导致普通物料出现非预期负库存；质量检查捕获后改为“无库存先入库、出库不超过余额”，只保留显式阻断场景。
- 结果模型最初把 `complete=true` 错误等同于“必须有数值”；按业务口径修正为零消耗时覆盖天数可以为 `null`，同时 `infinite_coverage=true` 且事实完整。
- FastAPI 生命周期测试最初未使用 `TestClient` 上下文，导致引擎未初始化；改为按真实启动方式进入 lifespan。
- 风险清单最初会把 Repository 异常吞成空列表；改为领域级 `RiskListResult`，严格区分 `ok`、`empty` 与 `error`。
- Phase 2 审计发现正常库存也被计入呆滞金额；改为先确定呆滞数量，再计算金额，并补齐保护期、零库存和阈值边界测试。
- 正常场景最初缺少需求对比前窗，只能证明证据不足；补齐稳定前窗消耗后，需求下降正例、反例与证据不足成为三个独立用例。

## 7. 简历与面试表述

只在对应证据产生后填写：

- 一句话项目价值：待验证后定稿。
- 个人职责：待开发后定稿。
- 量化成果：待测试和评测后填写。
- 最有价值的技术权衡：待阶段复盘后填写。
- 最困难问题及解决过程：待真实发生后填写。
