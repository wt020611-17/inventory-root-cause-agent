# 实习资料借鉴映射

## 1. 资料位置与盘点结论

只读来源：`D:\02金蝶\01业务需求`

- 文件总数：1374。
- 主要内容：需求分析文档、DC 表结构、本体模型 CSV、NGQL、Python 原子能力、生产延期链路资料。
- 其中 1155 个 JSON 属于本体模型导出包，不逐个阅读；需要时通过索引和结构化查询定位。
- `.qoder` 下内容视为自动生成的二级资料，只作导航，不作为事实依据。

## 2. 最值得借鉴的真实方法

### 需求到工具的六阶段管线

来源资料形成了完整的工程链路：

```text
原始需求
  -> 业务实体/字段/流程/规则抽取
  -> ERP 元数据锚定与流程探索
  -> 六维数据验证与 Issue 迭代
  -> 业务流程定稿
  -> DC 实体和字段映射
  -> 本体模型、图查询与原子能力
```

个人项目借鉴：在 `docs/` 中保留需求口径、实体关系、规则、验证记录和变更决策，不直接从代码开始。

### 验证驱动的链路收敛

实习资料中的验证维度包括：实体锚定、路径连通、字段闭合、主过滤键、跨系统连通、字典枚举。

个人项目借鉴：转换为以下自动化验证：

- 实体和字段 schema 校验。
- 图路径可达性测试。
- 关系两端节点完整性测试。
- 业务主键贯穿测试。
- 状态枚举覆盖测试。
- 空结果与异常状态测试。

### 三层系统排障

实习资料明确区分：关系数据源/加工层/图数据库。典型问题是“源表有数据，但图查询为空”，根因可能是 Schema 未注册、数据未加工或未导入，而不一定是查询语句错误。

个人项目借鉴：保持三层可替换：

```text
SQLite 事实数据 -> Graph Builder/Adapter -> NetworkX 或 Neo4j
```

并为每层建立独立健康检查和数据量核对。

### 多路径追溯与降级

生产延期资料采用“原生业务路径、直接关联路径、递归追踪路径、完整溯源路径”等多条候选链路，并通过覆盖率验证确定主路径和兜底路径。

个人项目借鉴：

- 主路径：库存事实 -> 来源单据 -> 生产订单。
- 捷径：库存事实 -> 生产订单直接关系。
- 兜底：通过来源追踪记录递归查找订单。
- 路径选择必须记录命中路径、跳数、证据和降级原因。

### 原子能力契约

现有 Python 能力普遍使用统一 `analyze(context)` 入口，并区分 `ok`、`empty`、`error`。

个人项目借鉴：升级为强类型 Tool 契约：

- Pydantic 输入模型。
- Pydantic 输出模型。
- 统一 `status/message/data/error/trace_id`。
- 区分“查询成功但无数据”和“查询执行失败”。
- 不以模拟结果冒充真实查询成功。

## 3. 资料到个人项目模块的映射

| 实习资料类别 | 提炼的方法 | 个人项目落点 |
|---|---|---|
| 01～04 需求分析文档 | 实体、字段、流程、规则和验证收敛 | `docs/business-requirements.md` |
| DC 实体映射 | 业务语义到数据表字段的映射 | SQLAlchemy 模型与数据字典 |
| 本体模型 CSV | Vertex/Edge/Property 建模约束 | `graph/schema.py` |
| NGQL v2/v4 | 多跳路径、可选路径、结果择优 | Graph Adapter 与路径策略 |
| CRC 原子能力 | 链路工具与根因工具拆分 | `tools/trace.py`、`tools/root_cause.py` |
| 生产延期资料 | 日期、状态、关闭条件判定 | 生产延期归因规则 |
| 链路诊断脚本 | 分段探针和最小链路定位 | 健康检查与诊断命令 |

## 4. 个人项目中重新设计的公开模型

为避免复制公司内部资产，项目统一改用自建模型：

- `InventoryRecord`
- `Material`
- `PurchaseOrder`
- `ProductionOrder`
- `SalesOrder`
- `EngineeringChange`
- `SourceDocumentLink`
- `Warehouse`

状态枚举、字段名、样例 ID、数据量和业务阈值全部重新设计；数据由项目脚本生成。

## 5. 对原方案的增强

根据实习资料，项目方案增加以下亮点：

1. 图查询不只有一条硬编码路径，而是“主路径 + 捷径 + 兜底”的路径策略。
2. 增加图数据覆盖率报告，证明每条路径能够覆盖多少样例。
3. 增加分层诊断：事实表、图构建、图查询分别检查。
4. 增加 `empty` 与 `error` 的严格区分。
5. 根因报告必须记录命中条件、证据路径、原始状态和计算时间。
6. 评测不仅测 Agent，还测数据链路完整性和图路径可达性。

## 6. 面试中可讲的方法论

可以真实表达为：

> 在实习中，我接触了从原始业务需求、ERP 元数据映射、本体建模，到 NGQL 多跳查询和 Python 原子能力封装的完整链路。做个人项目时，我没有复制公司代码或数据，而是把这套方法重新实现为合成数据上的库存归因 Agent，并补充了强类型接口、自动化测试、路径覆盖率和 Agent 评测。

需要避免的表达：

- 把团队整体平台成果描述成个人独立开发。
- 使用内部客户名、真实表名、系统地址或生产数据量作为个人项目数据。
- 声称项目已经达到生产级高并发或已在真实客户环境上线。
