# 智能库存归因 Agent：项目学习与面试地图

## 1. 学习原则

不要先看完一套大而全的课程再开发。按“实现一个模块—理解原理—准备追问—写测试”的循环学习。每个知识点必须能回答：它在项目的哪个文件中使用、解决了什么问题、还有什么替代方案。

本项目面向 AI 应用工程师岗位，业务复杂度固定在五表三根因；学习重点是 Agent 编排、后端服务、结果校验、上下文管理、可观测性和评测。RAG、Embedding、Milvus、BM25 和 RRF 已由另一个 GraphRAG 项目承担，本项目不重复堆叠。

## 2. 第一优先级：必须掌握

### Python 工程化

需要学习：

- 类型注解、dataclass 与 Pydantic 的区别。
- 分层设计：API、Service、Repository、Domain。
- 自定义异常、统一错误处理、logging。
- 同步与异步函数，`async/await` 的适用边界。
- pytest、fixture、mock、参数化测试。

项目实践：

- 为所有 Tool 定义强类型输入输出。
- 让业务公式脱离 FastAPI 和 LangGraph，可以独立测试。
- 为数据库和 LLM 依赖建立接口，测试时替换为 fake。

高频面试题：

- Pydantic 校验发生在哪一层？
- 为什么不能把所有逻辑写在 FastAPI 路由里？
- async 是否一定比同步快？
- 如何测试依赖大模型的代码？
- 如何设计统一异常和错误码？

### FastAPI 与接口设计

需要学习：

- 路由、依赖注入、请求模型、响应模型。
- RESTful 基础、幂等性、状态码。
- 中间件、Trace ID、超时和限流概念。
- OpenAPI/JSON Schema 如何描述 Tool 接口。

项目实践：

- 实现 `/chat` 与 `/analysis` 两种入口。
- 用中间件生成 Trace ID。
- 对参数错误、数据不存在、工具超时给出不同响应。

高频面试题：

- 为什么同时保留自然语言入口和结构化入口？
- FastAPI 为什么性能较好？
- 如何处理一个耗时 30 秒的 LLM 请求？
- 如何保证接口幂等？

### Tool Calling 与结构化输出

需要学习：

- Tool Calling 的本质：把函数 schema 提供给模型，由模型生成函数名和参数。
- JSON Schema、Pydantic、结构化输出校验。
- Tool 描述如何影响工具选择。
- 参数校验、重试、拒绝执行和结果标准化。

项目实践：

- 为 `list_inventory_risks`、`analyze_material_root_cause`、`trace_evidence` 三个用例级 Tool 定义清晰、互不重叠的描述。
- 用评测集测试工具选择和参数抽取。
- 故意制造非法参数，验证系统不会直接执行。

高频面试题：

- Tool Calling 与普通 Prompt 输出 JSON 有什么区别？
- 模型选择了错误工具怎么办？
- 如何避免工具描述互相冲突？
- Tool 返回空结果时 Agent 应如何处理？

### LangGraph / Agent 工作流

需要学习：

- ReAct、Plan-and-Execute、Workflow 的区别。
- 状态、节点、条件边、循环和终止条件。
- 确定性流程与 Agent 自主决策的边界。
- 超时、最大步数、重试和降级。

项目实践：

- 库存分析使用固定工作流。
- 只有意图识别和报告组织使用 LLM。
- 设置最大执行步数，避免循环。
- LLM 不可用时返回结构化结果。
- 用最小会话上下文支持缺失仓库或分析日期的两轮补全，并设置 TTL。

高频面试题：

- 为什么不直接使用一个 ReAct Agent 完成全部任务？
- LangGraph 相比 LangChain Agent 解决了什么问题？
- 如何处理 Agent 死循环？
- 哪些节点必须是确定性的？

### SQL、库存指标与数据建模

需要学习：

- 表设计、主外键、索引、JOIN、聚合和窗口函数基础。
- Repository Pattern 与事务基础。
- 平均库存、库存周转率、覆盖天数、库龄。
- 缺失数据、零需求和异常值处理。

项目实践：

- 使用 SQLite 建立合成业务表。
- 用 SQL 查询事实数据，用 Python 计算和验证复杂指标。
- 对关键查询使用 `EXPLAIN` 理解索引作用。

高频面试题：

- 库存周转率的分子和分母是什么？
- 平均日需求为零时覆盖天数怎么处理？
- 为什么某个字段需要索引？
- 如何避免 N+1 查询？

### 知识图谱

需要学习：

- Vertex、Edge、Property 与关系型表的区别。
- 图建模、方向、基数、多跳查询和路径爆炸。
- NetworkX、Neo4j/NebulaGraph 的适用场景。
- 图谱检索和向量检索的边界。

项目实践：

- 设计采购单、生产单、物料和仓库四类节点及三类受控边。
- 实现最大跳数和节点数限制。
- 为图存储定义 Adapter，MVP 与具体数据库解耦。
- 能解释当前两跳场景可用 JOIN 完成，NetworkX 的价值是验证证据路径接口和后续扩展边界，而不是替代所有关系查询。

高频面试题：

- 为什么这个业务需要图，而不是多表 JOIN？
- 图谱如何帮助多跳归因？
- 如何避免图查询返回结果爆炸？
- Neo4j 与 NebulaGraph 有什么差别？回答时只说自己验证过的部分。

## 3. 第二优先级：项目加分项

### 评测体系

需要学习：工具选择准确率、参数准确率、任务完成率、Recall@K、MRR、faithfulness 的含义与适用范围。

项目重点不是把所有 RAG 指标都用一遍，而是选择与 Agent Tool 相关的指标：

- 工具选择准确率 = 正确工具选择数 / 总样例数。
- 参数准确率 = 关键参数完全正确的样例数 / 总样例数。
- 任务完成率 = 满足验收条件的任务数 / 总任务数。
- 证据正确率 = 有效且与结论相符的证据数 / 输出证据数。

高频面试题：

- 你如何证明 Agent 优化后变好了？
- 评测集如何构造，如何避免只覆盖正常问题？
- LLM 输出具有随机性，怎样保证评测可复现？

### 可观测性与稳定性

需要学习：结构化日志、Trace ID、延迟、成功率、Token 使用量、重试、熔断和降级概念。

项目实践：记录节点名称、耗时、工具名、结果状态，不记录模型私有思维过程或密钥。

高频面试题：

- 线上 Agent 回答错误，如何定位是模型、工具还是数据的问题？
- 什么请求可以重试，什么请求不能盲目重试？
- 如何控制 LLM 成本和延迟？

### Docker 与部署

需要复盘：镜像、容器、Dockerfile 分层、环境变量、端口、健康检查和 volume。

项目实践已完成：同一非 root 镜像运行 FastAPI/Streamlit，Compose 管理端口、健康检查和合成 SQLite 卷；空白密钥自动降级，API/UI 均完成真实 healthy 验收。个人独立操作掌握程度仍按教学知识库记录。

高频面试题：

- 镜像与容器有什么区别？
- 为什么要设置 `.dockerignore`？
- 容器启动成功为什么不代表服务可用？

## 4. 第三优先级：了解即可

- JWT、RBAC、行级/字段级权限的整体思路。
- Redis 缓存、消息队列、高并发和服务拆分。
- MCP Server/Client/Tool 的协议角色。
- RAG、Embedding、BM25、RRF 与 Rerank。
- Neo4j 或 NebulaGraph 的生产部署。

这些内容可以在 README 的扩展设计中体现，但 MVP 不应为了堆技术栈全部实现。

## 5. 项目开发与学习日程

| 天数     | 开发输出                     | 当天学习               | 当天必须能回答           |
| ------ | ------------------------ | ------------------ | ----------------- |
| Day 1  | 业务口径、数据模型、工程骨架           | 库存指标、分层架构、Pydantic | 为什么公式不能交给 LLM？    |
| Day 2  | SQLite、Repository、查询 API | SQL、索引、依赖注入        | 为什么需要 Repository？ |
| Day 3  | 指标 Tool 与单测              | pytest、边界值、异常设计    | 零需求怎样处理？          |
| Day 4  | 需求下降、超量采购规则与证据       | 时间窗口、规则证据          | 为什么根因分数不是概率？     |
| Day 5  | 生产延期规则与最小证据图          | 日期/状态建模、多跳查询      | 如何避免生产模型膨胀？       |
| Day 6  | FastAPI 完整接口             | 中间件、Trace ID、响应模型  | 如何定位一次失败请求？       |
| Day 7  | Agent 工作流与上下文补全          | Workflow、状态、TTL      | 为什么限制 Agent 自主性？  |
| Day 8  | 重试、降级、结果校验              | 可靠性、幂等、超时          | LLM 挂了系统还能做什么？    |
| Day 9  | 评测集与基线报告                | Agent Evaluation   | 如何量化工具调用效果？       |
| Day 10 | Docker、README、演示与面试       | 部署、STAR、项目复盘       | 项目最难的问题是什么？       |

## 6. 项目介绍模板

### 30 秒版本

我结合企业 ERP 语义映射、知识图谱和 Analyze Tool 的实习经验，独立实现了一个智能库存异常归因 Agent。系统用 FastAPI 提供服务，通过三个用例级 Tool 完成风险清单、单物料归因和证据追踪，并用确定性代码识别需求下降、超量采购和生产延期。LLM 只负责参数理解、工具选择和报告组织，系统还支持证据校验、两轮参数补全、无模型降级和可复现评测。

### 两分钟版本应覆盖

1. 为什么选库存归因场景。
2. 数据模型和核心业务口径。
3. Tool 与 Agent 工作流如何分工。
4. 为什么需要图谱多跳证据。
5. 如何处理空数据、错误工具和 LLM 不可用。
6. 测试与评测结果。
7. 自己完成了什么、下一步如何扩展。

## 7. 简历指标的生成规则

项目完成前不要预写漂亮数字。完成后从以下事实中选择：

- 自动化测试数量与通过率。
- 评测集样例数量。
- 工具选择准确率与参数准确率。
- 端到端任务完成率。
- 平均/分位响应时间（注明环境和样本数）。
- 支持的归因类型和图关系数量。

所有数字必须能通过测试命令或评测脚本重新生成。

## 8. 面试前自检

- 能不看代码画出完整架构。
- 能解释每张表、每条图关系和每个指标。
- 能说明为什么没有把全部逻辑交给 LLM。
- 能现场讲一个成功案例和一个失败/降级案例。
- 能解释 Tool schema、状态流转和终止条件。
- 能指出项目当前局限，不夸大为生产系统。
- 能从测试报告中找到简历上的每个数字。
- 能明确区分个人项目与实习中的真实贡献。

## 9. Phase 4 已验证事实速查

| 主题 | 已验证事实 | 证据或复现入口 | 面试表达边界 |
|---|---|---|---|
| 自动化测试 | 230 项通过 | `python -m pytest -p no:cacheprovider` | 只说当前仓库测试数，不泛化为生产覆盖 |
| 分支覆盖率 | 91.29%，门槛 90% | `reports/coverage.json`、`pyproject.toml` | 说明统计对象为 `app` |
| 容器交付 | API/UI 均 healthy；无密钥 Agent `ok`；UID 999 非 root | `reports/container_verification.json` | 表达为本机 Docker Desktop 验收，不泛化为生产集群 |
| Agent 评测 | 15 条样例；工具选择、参数抽取、任务完成均 15/15，证据完整 8/8，安全降级 5/5 | `python -m evals.runners.agent_eval` | 说明是版本化合成离线集，不声称代表线上准确率 |
| 性能 | 30 次最终本地测量；结构化最大 7.704 ms、证据图最大 6.960 ms、无 LLM Agent 最大 81.723 ms | `python -m benchmarks.run_performance` | 不包含 LLM 网络延迟，不编造优化百分比 |
| 冷启动 | Python 3.11.9 全新环境完成安装、初始化、测试、API/Streamlit 和无 LLM 请求 | `reports/cold_start_verification.json` | 表达为本机 Windows 复现，不等同于生产部署 |
| 安全边界 | 安全扫描 0 项问题；日志不记录正文、密钥、提示词或私有推理 | `python scripts/security_scan.py` | 不宣称具备完整生产安全体系 |

### Phase 4 高频追问

- 为什么覆盖率门槛是 90%：先生成真实分支覆盖率基线，最终为 91.29%，再把门槛设为 90%，既能阻止明显回退，也给不同 Python/依赖环境留下小幅分支差异空间。
- 为什么离线评测可以复现：样例、固定 seed、期望工具、参数、状态和证据条件都版本化；默认关闭 LLM 网络调用，评测聚焦可确定判断的 Agent 行为。
- 为什么性能报告不测 DeepSeek：网络、供应商排队和模型生成长度会混入不可控变量；当前报告用于定位本地 Service、图查询和无 LLM 工作流成本，模型端延迟必须另建真实调用基线。
- 如何证明降级不是“假成功”：响应公开 `llm_used=false`，结构化结果仍经过 Tool、Service 和证据校验；没有数据返回 `empty`，质量不满足返回 `blocked`，不会统一包装为 `ok`。
