# 呆滞料智能体：简历与面试材料

## 简历项目区

### 项目名称与技术栈

**呆滞料智能体｜个人项目**

Python、FastAPI、Pydantic、SQLAlchemy、SQLite、LangGraph、NetworkX、Streamlit、pytest、Docker Compose

### 一句话业务价值

将库存流水、采购单和生产单的人工交叉排查收敛为可重复执行的只读 Agent，输出呆滞指标、三类候选根因及可核对的单据与关系路径。

### 三条核心项目描述

- 设计五表固定 seed 合成数据与确定性库存分析服务，计算无消耗天数、覆盖天数、呆滞数量/金额，并以可配置规则识别需求下降、超量采购和生产延期；严格区分 `ok`、`empty`、`blocked`、`error`。
- 基于 LangGraph 编排风险清单、单物料归因、证据追踪三个强类型 Tool，实现参数抽取、两轮上下文补全、证据校验、有限重试和无模型降级；LLM 仅负责意图理解与语言组织，不能修改指标、根因得分或证据。
- 建立 230 项自动化测试与 90% 分支覆盖率门槛；15 条版本化 Agent 评测中工具选择 12/12、参数抽取和任务完成均 15/15，安全降级 5/5；30 次最终本地无 LLM Agent 测量最大 81.723 ms，并完成 Python 3.11.9 冷启动、4 场景自动演示和 Docker API/UI 双服务验收。

### 使用边界

- 项目只使用纯合成数据，不写“接入真实 ERP”或“生产落地”。
- 评测指标必须带样例规模，不能表述为开放域准确率 100%。
- 81.723 ms 是固定 seed、本地、无 LLM 链路最大值，不包含 DeepSeek 网络延迟。
- 不写未经测量的效率提升、成本下降或业务金额收益。

## 高频面试问答

### 1. 为什么不用 LLM 直接算库存指标？

库存指标涉及日期窗口、零消耗、缺失成本和金额精度，必须可重复、可测试、可审计。LLM 对同一输入可能产生不同数值，也无法替代数据库事实约束。因此我把库存计算、规则命中和根因得分放在独立 Service 中，由 pytest 验证正常、边界、空值和非法场景；LLM 只处理自然语言参数和结果表达。这样即使模型不可用，结构化分析仍能工作。

### 2. 如何区分 `empty` 和 `error`？

`empty` 表示请求成功执行，但没有匹配事实，例如物料在指定仓库和日期没有库存流水；它可以返回 200 和空结果说明，不能附带虚构指标。`error` 表示 Repository、Tool 或内部执行失败，需要稳定错误类别和 Trace ID，不能夹带业务结论。项目还单独定义 `blocked`：事实已读取，但数据质量或证据不足，允许保留事实、禁止输出确定性根因。

### 3. 如何防止 LLM 幻觉修改业务事实？

我设置了四层边界：第一，Tool 输入输出由 Pydantic 严格校验并拒绝未知字段；第二，数值与根因只由确定性 Service 生成；第三，LangGraph 在组织回答前校验证据，证据不足时进入阻断或降级；第四，LLM 生成的摘要不能回写结构化结果。响应公开 `llm_used`、工具名和动作摘要，日志记录 Trace ID 和受控元数据，但不暴露提示词或 Chain-of-Thought。

### 4. 为什么首期用 NetworkX，而不是 Neo4j？

MVP 只有四类节点、三类关系、最多两跳，数据规模也很小，直接引入外部图数据库会增加部署和运维成本。NetworkX 足以验证证据图的数据模型、路径输出合同以及最大跳数、50 节点和 100 ms 限制。当前 JOIN 也能完成主要事实查询；等到真实数据量、多人共享、持久化图查询和复杂多跳成为实际需求，再通过图适配器迁移到 Neo4j 或其他图数据库。

### 5. 如何从合成数据迁移到真实 ERP？

我会分五步迁移：先冻结当前领域模型和口径，把 ERP 字段映射到规范化输入；再通过只读 Repository 适配器接入脱敏测试库或数据服务，不让业务 Service 依赖具体表名；随后运行引用完整性、数量、日期和对账质量门；然后用影子模式把旧报表与 Agent 指标逐条对账，并由业务确认阈值；最后再补权限、审计、密钥管理、监控和回滚。迁移初期只读，不自动执行调拨、报废或订单修改。

## STAR 三轮口述模拟

> 以下为三轮可直接朗读的模拟稿。已完成书面计时结构；真人练习时建议分别控制在 45 秒、90 秒和 120 秒以内。

### 第一轮：45 秒基础版

**S/T：** 制造业排查呆滞库存时，需要跨库存流水、采购单和生产单核对，单纯返回高库存缺少根因和证据。我希望做一个可复现、不会让模型篡改业务事实的个人项目。

**A：** 我用五张合成业务表建立固定 seed 数据，以确定性 Service 计算库存指标和三类根因，再用 LangGraph 编排三个强类型 Tool。LLM 只做参数理解和语言组织，无密钥时自动降级。

**R：** 项目现有 230 项测试，分支覆盖率 91.29%；15 条 Agent 评测中参数抽取和任务完成均 15/15，自动演示四个场景以及 Docker API/UI 双服务验收全部通过。

**本轮复盘：** 信息完整、数字可追溯；面试现场不要继续堆技术名词，等待追问。

### 第二轮：90 秒技术版

**S：** 呆滞库存排查的难点不是识别库存数量，而是把无消耗天数、呆滞金额与采购、生产事实关联起来，同时避免 Agent 为了回答完整而编造根因。

**T：** 我的目标是实现一个只读 MVP，覆盖风险清单、单物料归因和证据追踪，并保证没有 LLM 时核心分析仍可执行。

**A：** 我把系统拆成 Domain、Repository、Service、Tool、Agent 和 API/UI。SQLAlchemy Repository 读取固定 seed SQLite；Service 计算指标并用规则识别需求下降、超量采购和生产延期；NetworkX 输出受限证据路径；LangGraph 处理参数补全、工具选择、证据校验、有限重试和降级。四种领域状态把成功、有事实但受阻、成功无数据和执行失败分开。

**R：** 当前 230 项测试通过，`app` 分支覆盖率 91.29%，门槛 90%。15 条离线评测中工具选择 12/12、参数抽取和任务完成 15/15；最终本地无 LLM Agent 连续 30 次最大 81.723 ms。所有数据均为合成数据，性能不包含模型网络延迟。

**本轮复盘：** 已说明分层和边界；如果被追问业务价值，回到“减少跨单据人工核对、输出可核对证据”，不要声称已经产生真实收益。

### 第三轮：120 秒压力追问版

**面试官：为什么这不是一个普通规则系统套了聊天界面？**

**回答（S/T）：** 规则系统负责的是确定性业务判断，但用户输入可能缺物料、仓库或日期，也可能在风险清单、根因分析和证据追踪之间切换。任务是让自然语言入口可靠地选择用例、补齐上下文，并在工具失败或模型不可用时保持状态一致。

**回答（A）：** 我没有使用开放式长链 ReAct，而是用 LangGraph 固定节点和条件边；Tool schema 由 Pydantic 限制，最多保留两轮必要上下文，证据校验失败不能输出确定性结论。NetworkX 只处理受限路径，数值和规则完全留在 Service。为了验证 Agent 部分，我把工具选择、参数、完成条件、证据和安全降级写成版本化评测集，而不只测试最终自然语言。

**回答（R）：** 在 15 条合成样例上，工具选择 12/12，参数抽取和任务完成 15/15，证据完整 8/8，安全降级 5/5；自动演示验证了正常、多根因、三条证据路径和空结果，全部在 `llm_used=false` 下通过。这个结果只说明当前固定业务范围可复现，我不会把它表述成生产环境准确率。

**本轮复盘：** 已正面回答“Agent 的必要性”，也主动交代评测边界；若继续追问扩展，说明先做 ERP 字段映射、影子对账和只读接入。

## 投递前核对

- 简历项目描述是否保留样例数、测试数和性能口径。
- 是否删除“生产落地”“显著提升”等无证据表述。
- 是否能从 `reports/`、`evals/results/` 和 `benchmarks/results/` 找到每个数字。
- 是否能在不看稿的情况下讲清确定性 Service 与 Agent 的职责边界。
- 是否能说明从合成数据迁移到 ERP 仍需字段映射、质量验收、权限和影子对账。
