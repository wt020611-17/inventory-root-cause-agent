# 呆滞料智能体：项目话术底稿

## 使用规则

- 这是表达底稿，不要求逐字背诵；先讲业务问题，再讲设计选择，最后用验证数字收口。
- 只使用本仓库已经实现并可复现的事实。真实企业数据、内部系统细节和未完成能力不进入话术。
- 评测结果必须说明样例规模和合成数据边界；性能结果必须说明不包含 LLM 网络延迟。

## 一句话版本

我独立实现了一个基于纯合成制造业数据的呆滞料归因 Agent，用确定性代码计算库存指标并识别需求下降、超量采购和生产延期，再由 LangGraph 编排三个强类型 Tool，输出可追溯到单据和关系路径的结构化结论。

## 30 秒版本

制造业排查呆滞库存时，只有“库存过高”还不够，业务人员需要继续核对库存流水、采购单和生产单。我做了一个只读库存归因 Agent：FastAPI 和 Streamlit 提供入口，LangGraph 负责参数补全和工具编排，三个用例级 Tool 分别处理风险清单、单物料归因和证据追踪。指标、规则和根因得分全部由确定性 Service 计算，LLM 只做理解与语言组织；没有模型密钥时仍能返回结构化结果，并严格区分 `ok`、`empty`、`blocked` 和 `error`。

## 90 秒版本

这个项目解决的是呆滞库存排查链路长、结论缺少证据的问题。第一版把业务范围控制在五张表、三类根因和三个 Tool，并且只使用固定 seed 的合成数据。

系统先通过 Agent 解析物料、仓库和分析日期；字段不完整时支持两轮补全。Tool 调用后，Repository 读取 SQLite 事实数据，Service 计算无消耗天数、覆盖天数、呆滞数量和金额，再依据固定规则识别需求下降、超量采购和生产延期。NetworkX 只承担受限的证据路径查询，设置最大跳数、节点数和超时，避免把图数据库包装成万能方案。

我把确定性事实和模型能力做了明确隔离：LLM 不能修改指标、根因得分或证据；模型不可用时，系统切换到确定性解析和模板输出，并公开 `llm_used=false`。结果为空时返回 `empty`，数据质量不足时返回 `blocked`，不会为了回答完整而编造结论。

工程验证方面，当前有 227 项自动化测试，`app` 分支覆盖率 91.27%，门槛 90%；15 条版本化 Agent 评测样例中，工具选择、参数抽取和任务完成均为 15/15。固定 seed 最终本地性能基线连续测量 30 次，无 LLM Agent 最大耗时 77.630 ms；这个数字不包含模型网络延迟。项目也完成了 Python 3.11.9 全新环境冷启动、Ruff、依赖检查、安全扫描和 4 场景自动化演示验收。

## 架构讲解顺序

1. 入口：Streamlit 用于演示，FastAPI 提供自然语言和结构化接口。
2. 编排：LangGraph 管理输入解析、缺参、Tool 选择、执行、证据校验和降级。
3. Tool：固定为风险清单、单物料归因、证据追踪三个用例级入口。
4. 业务：Service 独立计算指标与规则，LLM 不参与数值决策。
5. 数据：Repository 隔离 SQLite；固定 seed 合成数据保证测试和演示可复现。
6. 证据：NetworkX 表达采购单、生产单、物料和仓库之间的受限路径。
7. 输出：统一返回状态、指标、候选根因、证据、动作摘要和 Trace ID。

## 三个演示案例怎么讲

### 正常物料

输入 `MAT-SYN-NORMAL × WH-SYN-01 × 2026-03-31`。重点不是“必须找出根因”，而是系统可以返回 `ok` 和指标，同时在规则不命中时保持根因列表为空。

### 多根因物料

输入 `MAT-SYN-MULTI × WH-SYN-01 × 2026-03-31`。系统稳定给出超量采购和生产延期，并展示采购单、生产单和命中规则。重点强调排序与分数来自确定性规则，不是模型主观判断。

### 空结果与降级

输入 `MAT-SYN-EMPTY × WH-SYN-01 × 2026-03-31`，系统返回 `empty`；关闭 LLM 后重新分析多根因物料，结构化事实保持可用且 `llm_used=false`。这两个场景分别证明数据状态语义和模型降级能力。

## 关键设计追问

### 为什么不让 LLM 直接计算库存指标？

库存公式和阈值结论需要精确、可测试、可审计。LLM 适合处理自然语言歧义和组织说明，不适合充当数值计算器。把计算留在 Service 后，同一套事实可以独立测试，也能在无模型时继续工作。

### 为什么是固定工作流，不是开放式 ReAct？

MVP 只有三个明确用例，执行顺序和安全边界可以预先定义。固定工作流更容易限制最大步数、校验证据和复现失败；开放式自主循环会增加错误工具、重复调用和不可控成本。

### 为什么同时保留结构化接口和自然语言接口？

结构化接口便于系统集成、回归测试和问题定位；自然语言接口降低业务使用门槛。两者最终复用同一套 Service 和结果模型，避免两套业务逻辑漂移。

### 为什么使用 NetworkX，而不是直接上 Neo4j？

当前数据规模小、证据路径最多两跳，关系数据库 JOIN 也能完成查询。NetworkX 的价值是先验证图节点、关系、路径返回合同和规模限制；只有数据规模、多人共享和持久化图查询成为真实需求时，才值得替换为外部图数据库。

### 如何防止 Agent 幻觉修改业务事实？

Tool 使用严格 Pydantic 输入输出；指标和根因由确定性代码生成；工作流在输出前校验证据；响应公开动作摘要而不暴露私有推理。LLM 只能基于结构化结果组织语言，不能回写计算值。

### 线上回答错误时怎么定位？

先用 Trace ID 关联 API 和 Tool 日志，再看参数抽取、所选 Tool、结果状态和错误类别。因为日志只记录受控元数据，不记录请求正文、密钥、提示词或 Chain-of-Thought，所以排查能力和隐私边界可以同时保留。

## 真实性边界

- 项目是个人可运行 MVP，不宣称已经接入真实 ERP 或承担生产流量。
- 评测集为 15 条版本化合成样例，不代表开放域线上准确率。
- 性能基线只覆盖本地结构化、图和无 LLM 链路，不包含 DeepSeek 网络延迟。
- 权限、容器化、外部图数据库、生产监控和真实数据迁移属于后续扩展。

## 证据入口

- 测试与覆盖率：`python -m pytest -p no:cacheprovider --cov=app --cov-report=term-missing`
- Agent 评测：`python -m evals.runners.agent_eval`
- 性能基线：`python -m benchmarks.run_performance`
- 安全扫描：`python scripts/security_scan.py`
- 冷启动记录：`reports/cold_start_verification.json`
